(function(){
'use strict';
if(window.__NS_V421_ADMIN_REVIEW_QUEUE__)return;
window.__NS_V421_ADMIN_REVIEW_QUEUE__=true;
const Store=window.NSV421Store;if(!Store)return;
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)],esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function role(){const b=window.NSV421ProductionBridge,d=b?.identityDiagnostic?.()||{};return b?.isProduction?(d.role||b.role||'Unknown'):($('#apRole')?.value||'Owner');}
function canReview(){return ['Owner','Admin'].includes(role());}
function canSubmit(){return ['Owner','Admin','Content'].includes(role());}
function canView(){return ['Owner','Admin','Content'].includes(role());}
function toast(msg){const e=$('#apToast');if(!e)return;e.textContent=msg;e.classList.add('is-show');clearTimeout(toast.t);toast.t=setTimeout(()=>e.classList.remove('is-show'),3600);}
function actor(){const b=window.NSV421ProductionBridge,d=b?.identityDiagnostic?.()||{};return d.email||d.uid||role();}
function reviewState(v){v=String(v||'').toLowerCase();if(v==='in_review'||v==='needs review'||v==='needs enhancement'||v==='needs changes')return 'Needs review';if(v==='rejected')return 'Rejected';if(v==='approved'||v==='live')return 'Approved';return 'Draft';}
function pending(s){const out=[];(s.media||[]).forEach(m=>{const st=reviewState(m.status);if(st==='Needs review')out.push({type:'Media',id:m.id,title:m.name||m.id,status:m.status||st,preview:m.thumb||m.src||'',target:m.reviewTarget||m.placement||'Library only',impact:m.publicAllowed===false?'Blocked from public use':'Can publish only after approval',submittedBy:m.reviewSubmittedBy||m.owner||'—',submittedAt:m.reviewSubmittedAt||'—',raw:m});});(s.content||[]).forEach(c=>{if(reviewState(c.state)==='Needs review')out.push({type:'Content',id:c.id,title:c.title||c.id,status:c.state,target:c.page||'—',impact:'Content remains unpublished until approval',submittedBy:c.reviewSubmittedBy||c.owner||'—',submittedAt:c.reviewSubmittedAt||'—',raw:c});});return out;}
function ensureStyle(){if($('#apV44ReviewStyle'))return;const st=document.createElement('style');st.id='apV44ReviewStyle';st.textContent=`.ap-v44-review-list{display:grid;gap:12px}.ap-v44-review-row{display:grid;grid-template-columns:92px minmax(0,1fr) auto;gap:14px;align-items:center;padding:12px;border:1px solid rgba(255,255,255,.1)}.ap-v44-review-preview{width:92px;height:92px;display:grid;place-items:center;background:#061006;overflow:hidden}.ap-v44-review-preview img{width:100%;height:100%;object-fit:contain}.ap-v44-review-meta{font-size:9px;line-height:1.6;color:rgba(255,255,255,.55)}.ap-v44-review-actions{display:flex;gap:6px;flex-wrap:wrap;justify-content:flex-end}.ap-v44-review-badge{margin-left:auto}@media(max-width:760px){.ap-v44-review-row{grid-template-columns:72px 1fr}.ap-v44-review-preview{width:72px;height:82px}.ap-v44-review-actions{grid-column:1/-1;justify-content:flex-start}}`;document.head.appendChild(st);}
function activate(){if(!canView()){toast(role()+' cannot access publishing reviews.');return false;}ensureSurface();$$('.ap-view').forEach(v=>v.classList.toggle('is-active',v.dataset.view==='reviews'));$$('#apNav button[data-view]').forEach(b=>b.classList.toggle('is-active',b.dataset.view==='reviews'));if(innerWidth<860)$('#apSidebar')?.classList.remove('is-open');render();return true;}
function ensureSurface(){ensureStyle();const nav=$('#apNav');if(nav&&!nav.querySelector('[data-view="reviews"]')){const media=nav.querySelector('[data-view="media"]');const b=document.createElement('button');b.type='button';b.dataset.view='reviews';b.innerHTML='<span class="ico">✓</span>Reviews <span class="pill ap-v44-review-badge" id="apReviewPill">0</span>';media?.parentElement?.insertBefore(b,media);}
 const navButton=nav?.querySelector('[data-view="reviews"]');if(navButton)navButton.hidden=!canView();
 let view=$('.ap-view[data-view="reviews"]');if(!view){view=document.createElement('section');view.className='ap-view';view.dataset.view='reviews';view.innerHTML='<div class="ap-page-head"><div><h1>Publishing Reviews</h1><p>One queue for submitted media and content. Nothing in review is public until Owner/Admin approves it.</p></div><button class="ap-btn" type="button" data-review-go="media">Open Media Library</button></div><div class="ap-card ap-panel"><div class="ap-panel-head"><div><h2>Needs review</h2><span id="apReviewSummary"></span></div></div><div class="ap-v44-review-list" id="apReviewList"></div></div>';$('.ap-content')?.appendChild(view);}return view;}
function render(){ensureSurface();const s=Store.load(),items=pending(s),pill=$('#apReviewPill'),sum=$('#apReviewSummary'),list=$('#apReviewList');if(pill)pill.textContent=String(items.length);if(sum)sum.textContent=items.length+' pending · '+role()+(canReview()?' can decide':' can submit/view');if(!list)return;list.innerHTML=items.map(x=>`<article class="ap-v44-review-row" data-review-type="${esc(x.type)}" data-review-id="${esc(x.id)}"><div class="ap-v44-review-preview">${x.preview?`<img src="${esc(x.preview)}" alt="">`:'<span>✦</span>'}</div><div><strong>${esc(x.type+' · '+x.id+' · '+x.title)}</strong><div class="ap-v44-review-meta">Status: ${esc(x.status)} · Submitted by: ${esc(x.submittedBy)} · Submitted: ${esc(x.submittedAt)}<br>Target: ${esc(x.target)} · Public impact: ${esc(x.impact)}</div>${x.raw.reviewNote?`<div class="ap-v44-review-meta">Reviewer note: ${esc(x.raw.reviewNote)}</div>`:''}</div><div class="ap-v44-review-actions">${canReview()?`<button class="ap-btn mini primary" data-review-action="approve">Approve</button><button class="ap-btn mini" data-review-action="changes">Request changes</button><button class="ap-btn mini danger" data-review-action="reject">Reject</button>`:'<span class="ap-status gold">Awaiting Owner/Admin</span>'}</div></article>`).join('')||'<div class="ap-empty">Nothing is waiting for review.</div>';}
function persist(s,action,target){Store.audit?.(s,action,target);Store.save(s);window.dispatchEvent(new CustomEvent('nsv421:change'));}
function decide(type,id,action){if(!canReview())return toast('Only Owner/Admin can approve or reject publishing reviews.');const s=Store.load(),obj=type==='Media'?Store.mediaById(s,id):(s.content||[]).find(x=>x.id===id);if(!obj)return toast('Review item no longer exists.');let note='';if(action!=='approve')note=window.prompt(action==='reject'?'Reason for rejection':'Changes requested (optional note):','')||'';obj.reviewedBy=actor();obj.reviewedAt=new Date().toISOString();obj.reviewNote=note;if(type==='Media'){if(action==='approve'){obj.status='Approved';obj.visible=true;}else if(action==='reject'){obj.status='Rejected';obj.visible=false;}else{obj.status='Needs review';obj.visible=false;obj.reviewOutcome='Needs changes';}}else{obj.state=action==='approve'?'APPROVED':action==='reject'?'REJECTED':'IN_REVIEW';obj.reviewOutcome=action==='changes'?'Needs changes':action;}persist(s,'Publishing review '+action,type+' '+id+(note?' · '+note:''));toast(type+' '+id+' '+(action==='approve'?'approved':action==='reject'?'rejected':'returned for changes')+'.');render();}
function openMedia(){const media=$('#apNav [data-view="media"]');if(!media)return toast('Media Library navigation is unavailable.');media.click();}
document.addEventListener('click',e=>{const a=e.target.closest?.('[data-review-action]');if(a){const row=a.closest('[data-review-id]');decide(row.dataset.reviewType,row.dataset.reviewId,a.dataset.reviewAction);return;}const nav=e.target.closest?.('#apNav [data-view="reviews"]');if(nav){e.preventDefault();e.stopImmediatePropagation();activate();return;}if(e.target.closest?.('[data-review-go="media"]')){e.preventDefault();openMedia();}},true);
window.NSV421ReviewQueue={open(){activate()},submitMedia(id,target){if(!canSubmit())return {ok:false,reason:'Current role cannot submit publishing reviews.'};const s=Store.load(),m=Store.mediaById(s,id);if(!m)return {ok:false,reason:'Media not found.'};m.status='Needs review';m.visible=false;m.reviewSubmittedBy=actor();m.reviewSubmittedAt=new Date().toISOString();m.reviewTarget=target||m.placement||'Library only';persist(s,'Media submitted for review',id+' → '+m.reviewTarget);render();return {ok:true};},reason(m){if(!m)return 'Media not found.';if(m.cat!=='People')return 'Wrong category: this rail accepts People media only.';if(m.status!=='Approved')return m.status==='Needs review'||m.status==='Needs enhancement'?'Needs review before it can be published.':'Status '+m.status+' is not publishable.';if(m.visible===false)return 'Media is hidden. Make it visible after approval.';if(m.publicAllowed===false)return 'Website use is disabled for this media.';if(!(m.src||m.thumb))return 'Media has no usable image source.';return '';},render,activate};
window.addEventListener('nsv421:change',()=>setTimeout(render,20));window.addEventListener('nsv421:production-ready',()=>setTimeout(render,20));document.addEventListener('change',e=>{if(e.target?.id==='apRole')setTimeout(render,0)},true);document.readyState==='loading'?document.addEventListener('DOMContentLoaded',render,{once:true}):render();
})();