(function(){
'use strict';
if(window.__NS_V421_ADMIN_V45__)return;window.__NS_V421_ADMIN_V45__=true;
const Store=window.NSV421Store;if(!Store)return;
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const CORE=new Set(['TENDER','GREEN','BULK']);
const production=()=>!!window.NSV421ProductionBridge?.isProduction;
const role=()=>$('#apRole')?.value||window.NSV421ProductionBridge?.role||Store.getSession?.()?.role||'Owner';
function toast(msg){const e=$('#apToast');if(!e)return;e.textContent=msg;e.classList.add('is-show');clearTimeout(toast.t);toast.t=setTimeout(()=>e.classList.remove('is-show'),3200);}
function modal(title,sub,html){const box=$('#apModal');if(!box)return;$('#apModalTitle').textContent=title;$('#apModalSub').textContent=sub||'';$('#apModalBody').innerHTML=html;box.classList.add('is-open');}
function closeModal(){$('#apModal')?.classList.remove('is-open','ap-modal-wide');}
function saveState(s,action,target){Store.audit?.(s,action,target||'');Store.save(s);toast(action);}
function go(view){document.querySelector(`#apNav button[data-view="${view}"]`)?.click();}
function core(p){return CORE.has(String(p?.sku||'').toUpperCase());}
function refs(s,p){const sku=String(p?.sku||'').toLowerCase(),name=String(p?.name||'').toLowerCase();return (s.orders||[]).filter(o=>{const h=[o.product,o.productName,o.productKey,o.sku].join(' ').toLowerCase();return (sku&&h.includes(sku))||(name&&h.includes(name));});}
async function persist(label){if(!production())return true;try{await window.NSV421ProductionBridge?.persist?.();toast(label+' saved to live configuration.');return true;}catch(e){showRemoteError(e,label);return false;}}
function productDetails(p){const s=Store.load(),inv=(s.inventory||[]).find(x=>String(x.sku).toUpperCase()===String(p.sku).toUpperCase()),count=refs(s,p).length;modal('Product details',p.sku,`<div class="ap-detail-grid"><div class="ap-detail"><label>Name</label><span>${esc(p.name)}</span></div><div class="ap-detail"><label>Lifecycle</label><span>${esc(p.archived?'Archived':p.state||'Draft')}</span></div><div class="ap-detail"><label>Public visibility</label><span>${String(p.state||'').toLowerCase()==='inactive'||p.archived?'Hidden':'Visible when storefront rules allow'}</span></div><div class="ap-detail"><label>Current stock</label><span>${inv?esc(inv.onHand):'Not recorded'}</span></div><div class="ap-detail"><label>Order references</label><span>${count}</span></div><div class="ap-detail"><label>Persistence</label><span>${core(p)?'Supported live catalogue SKU':'Draft-only; dynamic production SKU creation is not enabled'}</span></div></div><div class="ap-version-note" style="margin-top:12px"><strong>Delete safety</strong><br>${core(p)?'Live catalogue products are retained for order/audit history. Hide or archive them instead of hard deletion.':count?'Delete is blocked while an order references this draft.':'Owner can delete this unreferenced draft.'}</div>`);}
async function toggleProduct(sku,show){const s=Store.load(),p=(s.products||[]).find(x=>String(x.sku)===String(sku));if(!p)return;p.state=show?'Active':'Inactive';if(show)p.archived=false;p.updatedAt=new Date().toISOString();saveState(s,show?'Product shown':'Product hidden',p.sku);await persist('Product lifecycle');}
async function archiveProduct(sku){const s=Store.load(),p=(s.products||[]).find(x=>String(x.sku)===String(sku));if(!p)return;if(p.archived){p.archived=false;p.state='Active';saveState(s,'Product restored',p.sku);await persist('Product lifecycle');return;}const count=refs(s,p).length;if(count)return alert(`Archive blocked: ${count} order(s) reference ${p.sku}. Hide it instead.`);if(!confirm(`Archive ${p.sku}? The product will remain in history but be hidden.`))return;p.archived=true;p.state='Inactive';saveState(s,'Product archived',p.sku);await persist('Product lifecycle');}
function deleteDraft(sku){const s=Store.load(),p=(s.products||[]).find(x=>String(x.sku)===String(sku));if(!p||core(p))return;const count=refs(s,p).length;if(count)return alert(`Delete blocked: ${count} order(s) reference this draft.`);const typed=prompt(`Delete draft ${p.sku}? Type the exact SKU to confirm.`,'');if(typed===null||typed.trim()!==p.sku)return alert('Draft deletion cancelled.');s.products=s.products.filter(x=>x!==p);s.inventory=(s.inventory||[]).filter(x=>String(x.sku)!==String(p.sku));saveState(s,'Draft product deleted',p.sku);}
function decorateProducts(){const body=$('#apProductsBody');if(!body)return;const s=Store.load();[...body.querySelectorAll('tr')].forEach((tr,i)=>{const p=s.products?.[i],td=tr.lastElementChild;if(!p||!td)return;td.querySelectorAll('[data-v45-product]').forEach(x=>x.remove());const hidden=String(p.state||'').toLowerCase()==='inactive'||p.archived===true;const d=document.createElement('button');d.className='ap-btn mini';d.type='button';d.dataset.v45Product='details';d.textContent='Details';d.onclick=()=>productDetails(p);td.appendChild(d);const t=document.createElement('button');t.className='ap-btn mini';t.type='button';t.dataset.v45Product='toggle';t.textContent=hidden?'Show':'Hide';t.onclick=()=>toggleProduct(p.sku,!hidden);td.appendChild(t);if(role()==='Owner'){const b=document.createElement('button');b.className='ap-btn mini danger';b.type='button';b.dataset.v45Product='life';b.textContent=core(p)?(p.archived?'Restore':'Archive'):'Delete draft';b.onclick=()=>core(p)?archiveProduct(p.sku):deleteDraft(p.sku);td.appendChild(b);}});}
function addProduct(){if(production())return modal('Add product','Dynamic production SKUs are not enabled','<div class="ap-version-note"><strong>No fake live product will be created.</strong><br>The live checkout/catalog currently supports only TENDER, GREEN and BULK. New arbitrary SKUs require a separate dynamic-catalog backend/checkout change.</div>');modal('Add draft product','Local draft only',`<form class="ap-form" id="v45ProductAdd"><div class="ap-row"><div class="ap-field"><label>SKU</label><input name="sku" required></div><div class="ap-field"><label>Name</label><input name="name" required></div></div><div class="ap-row"><div class="ap-field"><label>Retail ₹</label><input name="retail" type="number" min="0" value="0"></div><div class="ap-field"><label>Minimum quantity</label><input name="moq" type="number" min="1" value="1"></div></div><button class="ap-btn primary">Save draft</button></form>`);$('#v45ProductAdd').onsubmit=e=>{e.preventDefault();const f=new FormData(e.currentTarget),sku=String(f.get('sku')||'').trim().toUpperCase(),s=Store.load();if(!sku||CORE.has(sku)||(s.products||[]).some(x=>String(x.sku).toUpperCase()===sku))return toast('SKU already exists or is reserved.');s.products.push({sku,name:String(f.get('name')||'').trim(),retail:+f.get('retail')||0,bulk:0,moq:Math.max(1,+f.get('moq')||1),priceVisible:false,state:'Draft',draft:true});closeModal();saveState(s,'Draft product created',sku);};}
function decorateInventory(){const body=$('#apInventoryBody');if(!body)return;const s=Store.load();[...body.querySelectorAll('tr')].forEach((tr,i)=>{const x=s.inventory?.[i],td=tr.lastElementChild;if(!x||!td||core(x)||role()!=='Owner')return;td.querySelectorAll('[data-v45-stock]').forEach(n=>n.remove());const b=document.createElement('button');b.className='ap-btn mini danger';b.type='button';b.dataset.v45Stock='delete';b.textContent='Delete draft';b.onclick=()=>{const n=Store.load(),row=n.inventory?.[i];if(!row||core(row))return;const used=(n.orders||[]).some(o=>[o.product,o.productName,o.productKey,o.sku].join(' ').toLowerCase().includes(String(row.sku).toLowerCase()));if(used)return alert(`Delete blocked: an order references ${row.sku}.`);if(confirm(`Delete inventory draft ${row.sku}?`)){n.inventory.splice(i,1);saveState(n,'Inventory draft deleted',row.sku);}};td.appendChild(b);});}
function addInventory(){if(production())return modal('Add SKU','Dynamic production inventory SKUs are not enabled','<div class="ap-version-note">Production stock is tied to TENDER, GREEN and BULK. A new live SKU requires the separate dynamic-catalog scope.</div>');modal('Add inventory draft','Local preview only',`<form class="ap-form" id="v45StockAdd"><div class="ap-row"><div class="ap-field"><label>SKU</label><input name="sku" required></div><div class="ap-field"><label>Name</label><input name="name" required></div></div><div class="ap-row"><div class="ap-field"><label>On hand</label><input name="on" type="number" min="0" value="0"></div><div class="ap-field"><label>Low-stock threshold</label><input name="low" type="number" min="0" value="20"></div></div><button class="ap-btn primary">Save draft SKU</button></form>`);$('#v45StockAdd').onsubmit=e=>{e.preventDefault();const f=new FormData(e.currentTarget),sku=String(f.get('sku')||'').trim().toUpperCase(),s=Store.load();if((s.inventory||[]).some(x=>String(x.sku).toUpperCase()===sku))return toast('SKU already exists.');s.inventory.push({sku,name:String(f.get('name')||'').trim(),node:s.warehouses?.[0]?.name||'Unassigned',onHand:+f.get('on')||0,reserved:0,incoming:0,low:+f.get('low')||0,freshness:'Draft / review',supplier:'Unassigned',draft:true});closeModal();saveState(s,'Inventory draft created',sku);};}
async function seoInfo(path){let url=path,res=await fetch(url,{cache:'no-store'}).catch(()=>null);if((!res||!res.ok)&&!/.html($|\?)/.test(url)){url=path.replace(/\/$/,'')+'.html';res=await fetch(url,{cache:'no-store'}).catch(()=>null);}if(!res||!res.ok)throw new Error('Page could not be loaded from this environment.');const doc=new DOMParser().parseFromString(await res.text(),'text/html');let sitemap='Not checked';try{const sm=await fetch('/sitemap.xml',{cache:'no-store'});if(sm.ok)sitemap=(await sm.text()).includes(new URL(url,location.origin).pathname)?'Included':'Not found in sitemap';}catch(e){}const schemas=[...doc.querySelectorAll('script[type="application/ld+json"]')].map(x=>{try{const j=JSON.parse(x.textContent||'{}');return Array.isArray(j)?j.map(y=>y['@type']):j['@type'];}catch(e){return null;}}).flat().filter(Boolean);return {title:doc.title||'Not recorded',description:doc.querySelector('meta[name="description"]')?.content||'Not recorded',canonical:doc.querySelector('link[rel="canonical"]')?.href||'Not recorded',robots:doc.querySelector('meta[name="robots"]')?.content||'Not recorded',schemas:schemas.join(', ')||'Not recorded',sitemap,h1:doc.querySelector('h1')?.textContent?.trim()||'Not recorded'};}
function decorateSeo(){const view=$('.ap-view[data-view="seo"]');if(!view)return;[...view.querySelectorAll('.ap-seo-row')].slice(1).forEach(row=>{if(row.querySelector('[data-v45-seo]'))return;const path=row.querySelector('span')?.textContent?.trim();if(!path)return;const b=document.createElement('button');b.className='ap-btn mini';b.type='button';b.dataset.v45Seo=path;b.textContent='View details';b.onclick=async()=>{modal('SEO details',path,'<div class="ap-empty">Loading page metadata…</div>');try{const x=await seoInfo(path);$('#apModalBody').innerHTML=`<div class="ap-detail-grid"><div class="ap-detail"><label>Title</label><span>${esc(x.title)}</span></div><div class="ap-detail"><label>Meta description</label><span>${esc(x.description)}</span></div><div class="ap-detail"><label>Canonical</label><span>${esc(x.canonical)}</span></div><div class="ap-detail"><label>Robots / indexing</label><span>${esc(x.robots)}</span></div><div class="ap-detail"><label>Structured data</label><span>${esc(x.schemas)}</span></div><div class="ap-detail"><label>Sitemap</label><span>${esc(x.sitemap)}</span></div><div class="ap-detail"><label>Primary H1 / topic</label><span>${esc(x.h1)}</span></div></div><div class="ap-version-note" style="margin-top:12px">Repository/page metadata only; no Google ranking claim.</div>`;}catch(e){$('#apModalBody').innerHTML=`<div class="ap-empty">${esc(e.message||e)}</div>`;}};row.appendChild(b);});}
function decorateOverview(){const view=$('.ap-view[data-view="overview"]');if(!view)return;const map={'Orders today':'orders','Revenue today':'payments','Active deliveries':'delivery','Content queue':'content'};view.querySelectorAll('.ap-kpi').forEach(c=>{const v=map[c.querySelector('.ap-kpi-label')?.textContent?.trim()];if(!v)return;c.tabIndex=0;c.setAttribute('role','button');c.style.cursor='pointer';c.onclick=()=>go(v);});const needs=[['payments','payment'],['followups','follow-up'],['content','story'],['delivery','delivery']];view.querySelectorAll('.ap-list-item').forEach(i=>{const t=(i.textContent||'').toLowerCase(),m=needs.find(x=>t.includes(x[1]));if(m){i.tabIndex=0;i.style.cursor='pointer';i.onclick=()=>go(m[0]);}});}
function decorateAnalytics(){const v=$('.ap-view[data-view="analytics"]');if(!v)return;if(!v.querySelector('[data-v45-analytics-note]')){const n=document.createElement('div');n.dataset.v45AnalyticsNote='1';n.className='ap-version-note';n.style.marginBottom='14px';n.innerHTML='<strong>Metric source</strong><br>These cards are planning/preview signals unless backed by a live analytics provider; they are not verified production analytics.';v.querySelector('.ap-page-head')?.after(n);}['orders','segments','products'].forEach((dest,i)=>{const c=v.querySelectorAll('.ap-grid-3 .ap-panel')[i];if(!c||c.querySelector('[data-v45-analytics-go]'))return;const b=document.createElement('button');b.className='ap-btn mini';b.type='button';b.dataset.v45AnalyticsGo=dest;b.textContent='View supporting data';b.onclick=()=>go(dest);c.appendChild(b);});}
function sectionRef(s,id){if((s.schedule||[]).some(x=>x.sectionId===id))return 'scheduled content references it';if(s.sectionMedia&&Object.prototype.hasOwnProperty.call(s.sectionMedia,id))return 'a media placement references it';if(Object.values(s.pageSequences||{}).some(v=>Array.isArray(v)&&v.includes(id)))return 'a page sequence references it';return '';}
function decorateSections(){const grid=$('#apCustomSectionGrid');if(!grid||role()!=='Owner')return;const s=Store.load();grid.querySelectorAll('.ap-v12-section-card').forEach(card=>{if(card.querySelector('[data-v45-section-delete]'))return;const title=card.querySelector('h3')?.textContent?.trim(),sec=(s.customSections||[]).find(x=>x.title===title);if(!sec)return;const host=card.querySelector('.ap-v12-section-actions')||card,b=document.createElement('button');b.type='button';b.className='ap-btn danger';b.dataset.v45SectionDelete=sec.id;b.textContent='Delete draft/custom';b.onclick=()=>{const n=Store.load(),item=(n.customSections||[]).find(x=>x.id===sec.id);if(!item)return;const reason=sectionRef(n,item.id);if(reason)return alert(`Delete blocked: ${reason}. Hide it or remove the reference first.`);const typed=prompt(`Delete custom section ${item.id}? Type the exact section ID.`,'');if(typed===null||typed.trim()!==item.id)return alert('Section deletion cancelled.');n.customSections=n.customSections.filter(x=>x.id!==item.id);saveState(n,'Custom section deleted',item.id);};host.appendChild(b);});}
let fb=null;async function firebase(){if(fb)return fb;const SDK='12.18.0',[appMod,authMod,fsMod]=await Promise.all([import(`https://www.gstatic.com/firebasejs/${SDK}/firebase-app.js`),import(`https://www.gstatic.com/firebasejs/${SDK}/firebase-auth.js`),import(`https://www.gstatic.com/firebasejs/${SDK}/firebase-firestore.js`)]),app=appMod.getApps()[0];if(!app)throw new Error('Production Firebase app is not ready.');const auth=authMod.getAuth(app),db=fsMod.getFirestore(app),user=auth.currentUser;if(!user)throw new Error('Admin authentication is required.');return fb={fsMod,db,user};}
function pick(v,...keys){for(const k of keys){const x=k.split('.').reduce((a,p)=>a?.[p],v);if(x!==undefined&&x!==null&&x!=='')return x;}return null;}
async function geocode(label){if(!label)return null;try{const r=await fetch('https://nominatim.openstreetmap.org/search?format=jsonv2&limit=1&q='+encodeURIComponent(label));if(!r.ok)return null;const a=await r.json();return a[0]?{lat:+a[0].lat,lng:+a[0].lon,label:a[0].display_name}:null;}catch(e){return null;}}
async function enrichOrders(){if(!production())return;try{const {fsMod,db}=await firebase(),q=fsMod.query(fsMod.collection(db,'orders'),fsMod.orderBy('createdAt','desc'),fsMod.limit(150)),snap=await fsMod.getDocs(q),by={};snap.forEach(d=>{const x=d.data()||{};by[x.orderId||d.id]={deliveryLat:pick(x,'deliveryLat','lat','delivery.lat','location.lat'),deliveryLng:pick(x,'deliveryLng','lng','delivery.lng','location.lng'),deliveryAddress:pick(x,'deliveryAddress','address','locationLabel','customerAddress','delivery.address'),deliveryPreference:x.deliveryPreference||x.deliveryMethod||'',deliveryLabel:x.deliveryLabel||'',handoverPointType:x.handoverPointType||'',handoverPointName:x.handoverPointName||'',handoverPointAddress:x.handoverPointAddress||'',handoverPointLat:x.handoverPointLat??null,handoverPointLng:x.handoverPointLng??null,handoverNote:x.handoverNote||'',trackingToken:x.trackingToken||'',customerUid:x.customerUid||'',status:x.status||'',eta:x.eta||x.deliveryEta||'',partnerName:x.partnerName||x.deliveryPartner||'',vehicle:x.vehicle||x.vehicleReference||'',customerUpdate:x.customerUpdate||''};});const s=Store.load();let changed=false;(s.orders||[]).forEach(o=>{if(by[o.id]){Object.assign(o,by[o.id]);changed=true;}});if(changed)Store.save(s);}catch(e){console.warn('[V45 order enrichment]',e);}}
function fillDelivery(d){const f=$('#apDeliveryForm');if(!f||!d)return;const set=(n,v)=>{if(f.elements[n]&&v!==undefined&&v!==null)f.elements[n].value=v;};set('status',d.status);set('eta',d.eta);set('partner',d.partnerName);set('vehicle',d.vehicle);set('location',d.locationLabel);set('lat',Number(d.lat).toFixed(6));set('lng',Number(d.lng).toFixed(6));set('update',d.customerUpdate);const head=$('.ap-view[data-view="delivery"] .ap-panel-head span');if(head)head.textContent=d.orderId||'No order selected';}
function deliverySource(){const map=$('#apAdminDeliveryMap');if(!map)return;let e=$('#nsV45DeliverySource');if(!e){e=document.createElement('div');e.id='nsV45DeliverySource';e.className='ap-version-note';e.style.margin='10px 0';map.before(e);}const d=Store.load().deliveryLive||{};e.innerHTML=`<strong>Selected order location</strong><br>${esc(d.orderId||'No order')} · ${esc(d.locationSource||'saved delivery pin')} · ${Number(d.lat||0).toFixed(6)}, ${Number(d.lng||0).toFixed(6)}<br><span style="opacity:.72">This is the saved delivery/checkout pin, not continuous GPS tracking.</span>`;}
async function openDelivery(orderId){const s=Store.load();let o=(s.orders||[]).find(x=>String(x.id)===String(orderId))||{};if(production()){try{const {fsMod,db}=await firebase(),snap=await fsMod.getDoc(fsMod.doc(db,'orders',orderId));if(snap.exists())o={...o,...snap.data()};}catch(e){console.warn('[V45 delivery read]',e);}}const pref=String(pick(o,'deliveryPreference','handoverPointType','deliveryMethod')||'').trim(),usesHandover=['pickup_hub','railway_station','bus_stop'].includes(pref),rawLat=usesHandover?pick(o,'handoverPointLat'):pick(o,'deliveryLat','lat','delivery.lat','location.lat'),rawLng=usesHandover?pick(o,'handoverPointLng'):pick(o,'deliveryLng','lng','delivery.lng','location.lng');let lat=rawLat===null?NaN:Number(rawLat),lng=rawLng===null?NaN:Number(rawLng),label=String(usesHandover?pick(o,'handoverPointAddress','handoverPointName'):pick(o,'deliveryAddress','address','locationLabel','customerAddress','delivery.address')||'').trim(),source=usesHandover?'customer-selected handover preference':'saved checkout/delivery pin';if(!Number.isFinite(lat)||!Number.isFinite(lng)){const g=await geocode(label);if(g){lat=g.lat;lng=g.lng;label=g.label||label;source='geocoded from saved address';}}if(!Number.isFinite(lat)||!Number.isFinite(lng)){lat=s.deliveryLive?.lat||23.1815;lng=s.deliveryLive?.lng||79.9864;source='fallback pin — verify before saving';}s.deliveryLive={...(s.deliveryLive||{}),orderId,status:String(pick(o,'status')||o.delivery||'confirmed').toLowerCase().replaceAll(' ','_'),eta:String(pick(o,'eta','deliveryEta')||s.deliveryLive?.eta||''),partnerName:String(pick(o,'partnerName','deliveryPartner')||s.deliveryLive?.partnerName||''),vehicle:String(pick(o,'vehicle','vehicleReference')||s.deliveryLive?.vehicle||''),locationLabel:label||`Pinned location · ${lat.toFixed(6)}, ${lng.toFixed(6)}`,lat,lng,customerUpdate:String(pick(o,'customerUpdate')||s.deliveryLive?.customerUpdate||''),internalNote:s.deliveryLive?.internalNote||'',trackingToken:pick(o,'trackingToken')||'',customerUid:pick(o,'customerUid')||'',locationSource:source};Store.save(s);fillDelivery(s.deliveryLive);go('delivery');setTimeout(()=>{fillDelivery(Store.load().deliveryLive);deliverySource();},150);}
async function saveDeliveryLive(){const d=Store.load().deliveryLive||{},id=d.orderId;if(!id)throw new Error('Select an order before saving delivery.');const f=$('#apDeliveryForm'),{fsMod,db,user}=await firebase(),ref=fsMod.doc(db,'orders',id),snap=await fsMod.getDoc(ref);if(!snap.exists())throw new Error(`Order ${id} no longer exists.`);const x=snap.data()||{},status=f.elements.status.value,lat=+f.elements.lat.value,lng=+f.elements.lng.value,label=f.elements.location.value,patch={status,eta:f.elements.eta.value,partnerName:f.elements.partner.value,vehicle:f.elements.vehicle.value,deliveryLat:lat,deliveryLng:lng,deliveryAddress:label,customerUpdate:f.elements.update.value,updatedAt:fsMod.serverTimestamp(),deliveryUpdatedBy:user.uid},batch=fsMod.writeBatch(db);batch.update(ref,patch);if(x.trackingToken){const r=fsMod.doc(db,'publicTracking',String(x.trackingToken)),ss=await fsMod.getDoc(r);if(ss.exists())batch.update(r,{status,eta:patch.eta,locationLabel:label,lat,lng,customerUpdate:patch.customerUpdate,updatedAt:fsMod.serverTimestamp()});}if(x.customerUid){const r=fsMod.doc(db,'customerOrders',String(x.customerUid),'orders',id),ss=await fsMod.getDoc(r);if(ss.exists())batch.update(r,{status,eta:patch.eta,locationLabel:label,lat,lng,updatedAt:fsMod.serverTimestamp()});}await batch.commit();const s=Store.load();Object.assign(s.deliveryLive,{status,eta:patch.eta,partnerName:patch.partnerName,vehicle:patch.vehicle,locationLabel:label,lat,lng,customerUpdate:patch.customerUpdate,locationSource:'saved delivery pin'});Store.save(s);return id;}
function identity(){const settings=$('.ap-view[data-view="settings"]');if(!settings)return;let c=$('#nsV45IdentityDiag');if(!c){c=document.createElement('div');c.id='nsV45IdentityDiag';c.className='ap-card ap-panel';c.style.marginTop='16px';settings.appendChild(c);}const d=window.NSV421ProductionBridge?.identityDiagnostic?.()||{};c.innerHTML=`<div class="ap-panel-head"><div><h2>Production identity diagnostic</h2><span>Use this when Firestore reports missing/insufficient permissions.</span></div><span class="ap-status ${production()&&d.isConfiguredOwner?'green':'gold'}">${production()?'Live':'Local'}</span></div><div class="ap-detail-grid"><div class="ap-detail"><label>Frontend role</label><span>${esc(role())}</span></div><div class="ap-detail"><label>Firebase email</label><span>${esc(d.email||'Not connected locally')}</span></div><div class="ap-detail"><label>Firebase UID</label><span>${esc(d.uid?d.uid.slice(0,8)+'…':'Not connected')}</span></div><div class="ap-detail"><label>Configured Owner match</label><span>${d.uid?(d.isConfiguredOwner?'Yes':'No'):'Not applicable'}</span></div></div><div class="ap-version-note" style="margin-top:12px">Identity/role mismatches must be fixed in Firebase configuration; this UI never weakens Firestore rules.</div>`;}
function showRemoteError(err,label='Live save'){const msg=String(err?.message||err||'Unknown Firestore error'),d=window.NSV421ProductionBridge?.identityDiagnostic?.()||{};let e=$('#nsV45RemoteError');if(!e){e=document.createElement('div');e.id='nsV45RemoteError';e.style.cssText='position:fixed;left:18px;bottom:18px;z-index:2147481500;max-width:min(520px,calc(100vw - 36px));padding:12px 14px;border:1px solid rgba(220,90,90,.45);background:#180909;color:#fff;font:11px/1.5 Inter,sans-serif;pointer-events:none';document.body.appendChild(e);}e.innerHTML=`<strong>${esc(label)} failed</strong><br>${esc(msg)}<br><span style="opacity:.7">Role: ${esc(role())} · Firebase identity: ${esc(d.email||'unknown')} · Owner match: ${d.uid?(d.isConfiguredOwner?'yes':'no'):'unknown'}</span>`;clearTimeout(showRemoteError.t);showRemoteError.t=setTimeout(()=>e.remove(),9000);}
function intercept(e){if(e.target.closest?.('#apProductAdd')){e.preventDefault();e.stopImmediatePropagation();addProduct();return;}if(e.target.closest?.('#apInventoryAdjust')){e.preventDefault();e.stopImmediatePropagation();addInventory();return;}if(e.target.closest?.('#apOrderDetail [data-go="delivery"]')){e.preventDefault();e.stopImmediatePropagation();const id=$('#apOrderDetail h3')?.textContent?.trim();if(id)openDelivery(id);return;}if(e.target.closest?.('#apSaveDelivery')&&production()){e.preventDefault();e.stopImmediatePropagation();toast('Saving delivery to live order and projections…');saveDeliveryLive().then(id=>toast(`${id} delivery saved to live order + projections.`)).catch(err=>showRemoteError(err,'Delivery save'));}}
function decorate(){decorateProducts();decorateInventory();decorateSeo();decorateOverview();decorateAnalytics();decorateSections();deliverySource();identity();}
function init(){document.addEventListener('click',intercept,true);window.addEventListener('nsv421:change',()=>setTimeout(decorate,30));window.addEventListener('nsv421:production-ready',()=>setTimeout(()=>{enrichOrders();decorate();},80));window.addEventListener('nsv421:remote-error',e=>showRemoteError(e.detail,'Live configuration save'));$('#apRole')?.addEventListener('change',()=>setTimeout(decorate,20));new MutationObserver(()=>{clearTimeout(init.m);init.m=setTimeout(decorate,40);}).observe(document.body,{childList:true,subtree:true});setTimeout(()=>{decorate();if(production())enrichOrders();},100);}
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init,{once:true}):init();
})();