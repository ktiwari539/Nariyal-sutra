(function(){
'use strict';
const S=window.NSV421Store,$=(q,r=document)=>r.querySelector(q),$$=(q,r=document)=>[...r.querySelectorAll(q)];
if(!S)return;
window.__NS_V421_ADMIN_V38__=1;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const local=()=>/^(localhost|127\.0\.0\.1|\[::1\])$/i.test(location.hostname);
const role=()=>$('#apRole')?.value||'Owner';
const actor=()=>$('#apUserName')?.textContent||role();
function st(){const s=S.load();s.communications=s.communications||[];return s;}
function toast(m){const e=$('#apToast');if(!e)return;e.textContent=m;e.classList.add('is-show');clearTimeout(toast.t);toast.t=setTimeout(()=>e.classList.remove('is-show'),2600);}
function modal(t,sub,b){$('#apModalTitle').textContent=t;$('#apModalSub').textContent=sub||'';$('#apModalBody').innerHTML=b;$('#apModal').classList.add('is-open','ap-modal-wide');}
function close(){$('#apModal')?.classList.remove('is-open','ap-modal-wide');}
function save(s,a,id){S.audit?.(s,a,id);S.save(s);window.dispatchEvent(new CustomEvent('nsv421:change'));toast(a);setTimeout(render,20);}
function uid(){return'COM-'+Date.now().toString(36)+'-'+Math.random().toString(36).slice(2,6);}
function manage(){return['Owner','Admin','Manager','Support','Sales'].includes(role());}
function approve(){return['Owner','Admin','Manager'].includes(role());}
function ready(c){if(c.channel==='Email')return/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(c.destination||'');if(c.channel==='WhatsApp')return/\d{8,15}/.test(String(c.destination||'').replace(/\D/g,''));return/^@?[A-Za-z0-9_]{5,32}$/.test(c.destination||'');}
function posture(c){if(!ready(c))return'Blocked · valid destination required';if(c.channel==='WhatsApp')return'Ready · opens prefilled WhatsApp; final send stays with the user';if(c.channel==='Telegram')return'Ready · opens the exact Telegram username; no phone mapping';if(local())return'Preview only · external email is blocked locally';return window.NS_ADMIN_EMAIL_ENDPOINT?'Ready · authenticated Admin email endpoint configured':'Gated · authenticated Admin email endpoint not configured';}
function cls(x){return/Sent|Approved/.test(x)?'green':/Failed/.test(x)?'gold':'gray';}
function render(){const h=$('#apCommunicationQueue');if(!h)return;const s=st();h.closest('.ap-card')?.style.setProperty('grid-column','1/-1');h.innerHTML='<div class="v38-toolbar"><input id="v38Q" class="ap-filter" placeholder="Search recipient, order or purpose"><select id="v38Status" class="ap-filter"><option>All statuses</option><option>Draft</option><option>Needs review</option><option>Approved</option><option>Sent</option><option>Failed</option></select><label><input id="v38Archived" type="checkbox"> Show archived</label></div><div id="v38Rows"></div>';
 function draw(){const q=($('#v38Q').value||'').toLowerCase(),f=$('#v38Status').value,arch=$('#v38Archived').checked;const a=s.communications.filter(c=>(arch||!c.archivedAt)&&(f==='All statuses'||c.status===f)&&(!q||[c.recipient,c.destination,c.orderId,c.purpose,c.subject].join(' ').toLowerCase().includes(q)));$('#v38Rows').innerHTML=a.map(c=>`<article class="v38-comm"><div class="v38-head"><div><strong>${esc(c.recipient||'Unnamed recipient')}</strong><span>${esc(c.channel)} · ${esc(c.purpose)}${c.orderId?' · '+esc(c.orderId):''}</span></div><span class="ap-status ${cls(c.status)}">${esc(c.archivedAt?'Archived · '+c.status:c.status)}</span></div><div class="v38-meta"><span>${esc(c.destination||'No destination')}</span><span>Owner: ${esc(c.owner||'Unassigned')}</span></div><div class="v38-provider">${esc(posture(c))}</div><div class="v38-actions"><button class="ap-btn mini" data-open="${c.id}">${['Draft','Failed'].includes(c.status)?'Edit':'Open'}</button><button class="ap-btn mini" data-preview="${c.id}">Preview</button><button class="ap-btn mini" data-dup="${c.id}">Duplicate</button>${c.status==='Draft'?`<button class="ap-btn mini" data-review="${c.id}">Submit for review</button>`:''}${c.status==='Needs review'&&approve()?`<button class="ap-btn mini primary" data-approve="${c.id}">Approve</button>`:''}${c.status==='Approved'?`<button class="ap-btn mini primary" data-send="${c.id}">${c.channel==='Email'?'Send email':'Open '+c.channel}</button>`:''}<button class="ap-btn mini" data-archive="${c.id}">${c.archivedAt?'Restore':'Archive'}</button>${c.status==='Draft'&&!c.sentAt&&role()==='Owner'?`<button class="ap-btn mini danger" data-delete="${c.id}">Delete draft</button>`:''}</div></article>`).join('')||'<div class="ap-empty">No communication records match this view.</div>';bind(s);}
 $('#v38Q').oninput=draw;$('#v38Status').onchange=draw;$('#v38Archived').onchange=draw;draw();
}
function bind(s){$$('[data-open]').forEach(b=>b.onclick=()=>composer(b.dataset.open));$$('[data-preview]').forEach(b=>b.onclick=()=>preview(b.dataset.preview));$$('[data-dup]').forEach(b=>b.onclick=()=>{const c=s.communications.find(x=>x.id===b.dataset.dup),n={...c,id:uid(),status:'Draft',sentAt:null,archivedAt:null,createdAt:new Date().toISOString(),createdBy:actor()};s.communications.unshift(n);save(s,'Communication duplicated',n.id);});$$('[data-review]').forEach(b=>b.onclick=()=>{const c=s.communications.find(x=>x.id===b.dataset.review);if(!ready(c)||!c.message?.trim())return toast('Complete a valid destination and message before review.');c.status='Needs review';save(s,'Communication submitted for review',c.id);});$$('[data-approve]').forEach(b=>b.onclick=()=>{const c=s.communications.find(x=>x.id===b.dataset.approve);if(!approve())return toast('This role cannot approve communications.');c.status='Approved';c.approvedAt=new Date().toISOString();c.approvedBy=actor();save(s,'Communication approved',c.id);});$$('[data-send]').forEach(b=>b.onclick=()=>send(b.dataset.send));$$('[data-archive]').forEach(b=>b.onclick=()=>{const c=s.communications.find(x=>x.id===b.dataset.archive);c.archivedAt=c.archivedAt?null:new Date().toISOString();save(s,c.archivedAt?'Communication archived':'Communication restored',c.id);});$$('[data-delete]').forEach(b=>b.onclick=()=>{const c=s.communications.find(x=>x.id===b.dataset.delete);if(role()!=='Owner'||c.status!=='Draft'||c.sentAt)return toast('Only Owner can delete an unsent draft.');if(!confirm(`Delete unsent draft for ${c.recipient}?`))return;s.communications=s.communications.filter(x=>x.id!==c.id);save(s,'Unsent communication draft deleted',c.id);});}
function composer(cid){if(!manage())return toast('This role cannot create or edit communications.');const s=st(),old=s.communications.find(x=>x.id===cid),c=old||{id:uid(),purpose:'General follow-up',recipient:'',destination:'',orderId:'',channel:'WhatsApp',subject:'',message:'',owner:actor(),scheduledAt:'',status:'Draft'};if(old&&!['Draft','Failed'].includes(old.status))return preview(old.id);modal(old?'Edit communication':'New communication','Saving never sends externally.',`<form class="ap-form" id="v38Form"><div class="ap-row"><div class="ap-field"><label>Purpose</label><input name="purpose" value="${esc(c.purpose)}"></div><div class="ap-field"><label>Channel</label><select name="channel">${['Email','WhatsApp','Telegram'].map(x=>`<option ${x===c.channel?'selected':''}>${x}</option>`).join('')}</select></div></div><div class="ap-row"><div class="ap-field"><label>Recipient</label><input name="recipient" required value="${esc(c.recipient)}"></div><div class="ap-field"><label>Destination</label><input name="destination" required value="${esc(c.destination)}"></div></div><div class="ap-field"><label>Subject</label><input name="subject" value="${esc(c.subject)}"></div><div class="ap-field"><label>Message</label><textarea name="message" required maxlength="2000">${esc(c.message)}</textarea></div><div class="v38-impact"><strong>Impact</strong><span>Save updates an internal draft only. Review and explicit approval are required before sending.</span></div><div class="v38-actions"><button type="button" class="ap-btn" id="v38PreviewDraft">Preview</button><button class="ap-btn primary">Save draft</button></div></form>`);const f=$('#v38Form'),vals=()=>Object.fromEntries(new FormData(f).entries());$('#v38PreviewDraft').onclick=()=>show({...c,...vals()});f.onsubmit=e=>{e.preventDefault();const v=vals();if(v.channel==='Email'&&!v.subject.trim())return toast('Email subject is required.');Object.assign(c,v,{status:'Draft',updatedAt:new Date().toISOString(),updatedBy:actor()});if(!old){c.createdAt=c.updatedAt;c.createdBy=actor();s.communications.unshift(c);}save(s,old?'Communication draft updated':'Communication draft created',c.id);close();};}
function show(c){modal('Communication preview',`${c.channel} · zero external sends`,`<div class="v38-preview"><small>Recipient</small><strong>${esc(c.recipient||'—')}</strong><span>${esc(c.destination||'—')}</span>${c.subject?`<h3>${esc(c.subject)}</h3>`:''}<p>${esc(c.message||'No message yet').split('\n').join('<br>')}</p><div class="v38-provider">${esc(posture(c))}</div></div>`);}
function preview(id){const c=st().communications.find(x=>x.id===id);if(c)show(c);}
async function send(id){const s=st(),c=s.communications.find(x=>x.id===id);if(!c||c.status!=='Approved')return toast('Communication must be approved first.');if(!ready(c))return toast('Destination is not ready.');if(c.channel==='WhatsApp'){window.open(`https://wa.me/${c.destination.replace(/\D/g,'')}?text=${encodeURIComponent(c.message)}`,'_blank','noopener');return save(s,'Approved WhatsApp handoff opened',c.id);}if(c.channel==='Telegram'){window.open(`https://t.me/${encodeURIComponent(c.destination.replace(/^@/,''))}`,'_blank','noopener');return save(s,'Approved Telegram handoff opened',c.id);}if(local())return toast('Preview only · external email is blocked locally.');if(!window.NS_ADMIN_EMAIL_ENDPOINT)return toast('Gated · authenticated Admin email endpoint not configured.');toast('Email provider is configured; explicit provider send remains protected by the production bridge.');}
function ownNewCommunication(e){const n=e.target.closest?.('#apCommunicationNew');if(!n)return;e.preventDefault();e.stopImmediatePropagation();composer();}
document.addEventListener('click',ownNewCommunication,true);
function init(){const css=document.createElement('style');css.textContent='.v38-toolbar,.v38-head,.v38-meta,.v38-actions{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.v38-toolbar{margin-bottom:12px}.v38-comm{border:1px solid #dfe5dd;border-radius:12px;padding:12px;margin:8px 0;background:#fff}.v38-head{justify-content:space-between}.v38-head div{display:grid;gap:3px}.v38-head span,.v38-meta,.v38-provider{font-size:10px;color:#667064}.v38-meta{margin:8px 0}.v38-provider{padding:7px 9px;background:#f5f7f4;border-radius:8px;margin:7px 0}.v38-impact{display:grid;gap:3px;padding:10px;border-radius:10px;background:#f7f3e8;font-size:10px;margin:10px 0}';document.head.appendChild(css);render();window.addEventListener('nsv421:change',()=>setTimeout(render,30));}
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',()=>setTimeout(init,180)):setTimeout(init,180);
})();