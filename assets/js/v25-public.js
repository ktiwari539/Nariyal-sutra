(function(){
'use strict';
const Store=window.NSV421Store, MediaDB=window.NSV421MediaDB;
const $=(s,r=document)=>r.querySelector(s);
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function eligible(s,id){const m=Store?.mediaById(s,id);return Store?.eligibleForPublic(s,m)?m:null;}
function sources(m){const id=m?.id||'';return [...new Set([m?.thumb,m?.src,`assets/images/ambassadors/thumbs/${id}.webp`,`assets/images/ambassadors/${id}.webp`,`assets/images/ambassador-preview/${id}.jpg`,`media-reference/candidates/${id}.webp`].filter(Boolean))];}
function card(m){const ss=sources(m),src=ss[0]||'assets/images/upload-placeholder.svg',blob=m.localBlobKey?` data-local-blob-key="${esc(m.localBlobKey)}"`:'';return `<article class="v25-story-person" data-id="${esc(m.id)}"><div class="v25-story-photo"><img src="${esc(src)}"${blob} alt="${esc(m.caption||m.name||'Nariyal Sutra community story')}" loading="lazy" decoding="async"></div><div class="v25-story-caption"><b>${esc(m.name||m.id)}</b>${m.caption?`<span>${esc(m.caption)}</span>`:''}</div></article>`;}
function wireFallback(root,s){root.querySelectorAll('.v25-story-person img:not([data-local-blob-key])').forEach(img=>{const m=Store.mediaById(s,img.closest('.v25-story-person')?.dataset.id),ss=sources(m);let i=Math.max(0,ss.indexOf(img.getAttribute('src')));img.onerror=()=>{i++;if(i<ss.length){img.src=ss[i];return;}const c=img.closest('.v25-story-person');if(c)c.remove();};});}
function streamSection(key,st,items){const reverse=st.direction==='rtl',anchor='people-'+key;const inner=items.map(card).join('');return `<section class="v25-story-stream" id="${esc(anchor)}" data-stream="${esc(key)}" style="--v25-speed:${+st.speed||48}s"><div class="v25-story-stream-head"><div><small>${key==='ambassadors'?'Ambassadors':key==='promoters'?'Promoters':'Community'}</small><h3>${esc(st.title||key)}</h3></div><p>${esc(st.subtitle||'')}</p></div><div class="v25-story-track ${reverse?'reverse':''}">${inner}${inner}</div><div class="v25-story-stream-foot">Full-picture treatment · hover to pause · Admin controls speed, direction and publication</div></section>`;}
function render(){if(!Store||!$('#v25PeopleStreams'))return;const s=Store.load(),streams=s.peopleStreams||{},host=$('#v25PeopleStreams');let html='';for(const key of ['ambassadors','promoters','community']){const st=streams[key]||{};if(st.enabled===false)continue;const ids=st.selectedIds||[],items=ids.map(id=>eligible(s,id)).filter(Boolean);if(!items.length)continue;html+=streamSection(key,st,items);}host.innerHTML=html;const world=host.closest('.v25-people-worlds');if(world)world.hidden=!html;if(MediaDB?.hydrate)MediaDB.hydrate(host);wireFallback(host,s);}
function addTelegramGateNote(){document.querySelectorAll('.v23-community-form').forEach(f=>{if(f.querySelector('.v25-telegram-note'))return;const t=document.createElement('div');t.className='v25-telegram-note';t.textContent='Telegram username is stored only as a contact preference until the official Nariyal Sutra bot/chat link is enabled.';const tg=f.querySelector('input[name=telegram]');tg?.parentElement?.appendChild(t);});}
function init(){render();addTelegramGateNote();addEventListener('nsv421:change',render);}
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();
})();
