(function(){
'use strict';
if(window.__NS_V421_ADMIN_V42_PEOPLE_STREAMS__)return;
window.__NS_V421_ADMIN_V42_PEOPLE_STREAMS__=true;
const Store=window.NSV421Store;if(!Store)return;
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const SDK='12.18.0',PROFILE_BG='#fbfaf5',DEFAULT_TEXT='#173d24';
const STREAMS=[
 {key:'ambassadors',name:'Brand Ambassadors',type:'Brand Ambassador',public:'People page → Brand Ambassadors rail'},
 {key:'promoters',name:'Brand Promoters & Community Voices',type:'Brand Promoter',public:'People page → Promoters rail'},
 {key:'community',name:'Community Spotlight',type:'Community',public:'People page → Community Spotlight rail'}
];
let active='ambassadors',remoteLoaded=false,remoteLoading=null;
function state(){const s=Store.load();s.peopleProfiles=s.peopleProfiles||{};return s;}
function bridge(){return window.NSV421ProductionBridge||null;}
function production(){return !!bridge()?.isProduction;}
function role(){const b=bridge(),d=b?.identityDiagnostic?.()||{};return b?.isProduction?(d.role||b.role||'Unknown'):($('#apRole')?.value||'Owner');}
function canManage(){return['Owner','Admin','Content'].includes(role());}
function eligible(m){return !!(m&&m.cat==='People'&&m.status==='Approved'&&m.visible!==false&&m.publicAllowed!==false&&(m.src||m.thumb));}
function toast(msg){const e=$('#apToast');if(!e)return;e.textContent=msg;e.classList.add('is-show');clearTimeout(toast.t);toast.t=setTimeout(()=>e.classList.remove('is-show'),3600);}
function save(s,a,t){Store.audit?.(s,a,t);Store.save(s);window.dispatchEvent(new CustomEvent('nsv421:change'));}
function hex(v){v=String(v||'').trim().toLowerCase();return /^#[0-9a-f]{6}$/.test(v)?v:null;}
function luminance(v){const h=hex(v);if(!h)return 1;const c=[1,3,5].map(i=>parseInt(h.slice(i,i+2),16)/255).map(x=>x<=.03928?x/12.92:Math.pow((x+.055)/1.055,2.4));return .2126*c[0]+.7152*c[1]+.0722*c[2];}
function contrast(a,b){const x=luminance(a),y=luminance(b),hi=Math.max(x,y),lo=Math.min(x,y);return(hi+.05)/(lo+.05);}
function safeTextColor(v){const h=hex(v);return h&&contrast(h,PROFILE_BG)>=4.5?h:DEFAULT_TEXT;}
async function firebase(){const [appMod,fsMod]=await Promise.all([import(`https://www.gstatic.com/firebasejs/${SDK}/firebase-app.js`),import(`https://www.gstatic.com/firebasejs/${SDK}/firebase-firestore.js`)]);let app=appMod.getApps()[0];if(!app){if(!window.NS_FIREBASE_CONFIG){await new Promise((res,rej)=>{const old=document.querySelector('script[data-ns-people-firebase]');if(old){old.addEventListener('load',res,{once:true});old.addEventListener('error',rej,{once:true});return;}const s=document.createElement('script');s.src='/firebase-config.js';s.dataset.nsPeopleFirebase='1';s.onload=res;s.onerror=rej;document.head.appendChild(s);});}if(!window.NS_FIREBASE_CONFIG)throw new Error('Firebase configuration is unavailable.');app=appMod.initializeApp(window.NS_FIREBASE_CONFIG);}if(typeof window.NS_INIT_APP_CHECK==='function')await window.NS_INIT_APP_CHECK(app);return{appMod,fsMod,app,db:fsMod.getFirestore(app)};}
async function waitProduction(){const b=bridge();if(!b?.isProduction)return null;const ready=await b.ready;if(ready?.error)throw new Error(ready.error);if(!b.user)throw new Error('Verified Admin authentication is required.');return b;}
async function publishProfiles(s){if(!production())return{preview:true};const b=await waitProduction();if(!['Owner','Admin','Content'].includes(role()))throw new Error(`${role()} cannot publish People profile details.`);const{fsMod,db}=await firebase(),payload={peopleProfiles:JSON.parse(JSON.stringify(s.peopleProfiles||{})),status:'live',type:'site-config',updatedAt:fsMod.serverTimestamp(),updatedBy:b.user.uid};await fsMod.setDoc(fsMod.doc(db,'contentStories','site-config'),payload,{merge:true});await fsMod.setDoc(fsMod.doc(db,'publicStories','site-config'),payload,{merge:true});window.dispatchEvent(new CustomEvent('nsv421:people-profiles-published',{detail:{count:Object.keys(s.peopleProfiles||{}).length}}));return{published:true,count:Object.keys(s.peopleProfiles||{}).length};}
async function loadRemoteProfiles(){if(!production()||remoteLoaded)return;if(remoteLoading)return remoteLoading;remoteLoading=(async()=>{try{await waitProduction();const{fsMod,db}=await firebase();let snap=await fsMod.getDoc(fsMod.doc(db,'contentStories','site-config'));if(!snap.exists()||!snap.data()?.peopleProfiles)snap=await fsMod.getDoc(fsMod.doc(db,'publicStories','site-config'));const profiles=snap.exists()?snap.data()?.peopleProfiles:null;if(profiles&&typeof profiles==='object'){const s=state();s.peopleProfiles=JSON.parse(JSON.stringify(profiles));Store.save(s);window.dispatchEvent(new CustomEvent('nsv421:change'));}remoteLoaded=true;}catch(e){console.warn('[People profile Admin sync]',e);toast('People profile sync warning: '+String(e?.message||e));}finally{remoteLoading=null;}})();return remoteLoading;}
function ensureStreams(s){s.peopleStreams=s.peopleStreams||{};STREAMS.forEach(d=>{const st=s.peopleStreams[d.key]||(s.peopleStreams[d.key]={});st.selectedIds=Array.isArray(st.selectedIds)?[...new Set(st.selectedIds)]:[];if(st.enabled===undefined)st.enabled=true;});}
function profile(s,m,def,create=false){let id=m.personId||m.faceGroup;if(!id&&create){id='PERSON-'+Date.now().toString(36)+'-'+Math.random().toString(36).slice(2,7);m.personId=id;}if(!id)return null;const p=s.peopleProfiles[id]||(create?(s.peopleProfiles[id]={personId:id,name:'',city:'',type:def.type,intro:'',story:'',feedback:'',rating:'',relatedMediaIds:[m.id],public:true,textColor:DEFAULT_TEXT}):null);if(p&&create&&!p.textColor)p.textColor=DEFAULT_TEXT;return p;}
function completeness(p){if(!p||!p.name)return'Missing details';if(!p.intro&&!p.story&&!p.feedback)return'Needs review';return'Complete';}
function modal(title,sub,body){$('#apModalTitle').textContent=title;$('#apModalSub').textContent=sub||'';$('#apModalBody').innerHTML=body;$('#apModal').classList.add('is-open','ap-modal-wide');}
function close(){$('#apModal')?.classList.remove('is-open','ap-modal-wide');}
function streamUse(s,id){ensureStreams(s);return STREAMS.filter(d=>(s.peopleStreams[d.key]?.selectedIds||[]).includes(id)).map(d=>d.name);}
function editPerson(mid,key){if(!canManage())return toast(role()+' cannot edit People profiles.');const s=state(),m=(s.media||[]).find(x=>x.id===mid),def=STREAMS.find(x=>x.key===key)||STREAMS[0];if(!m)return toast('Media record unavailable.');const p=profile(s,m,def,true),approved=(s.media||[]).filter(x=>x.cat==='People'&&x.status==='Approved'&&x.visible!==false&&x.publicAllowed!==false),textColor=safeTextColor(p.textColor);modal('Edit person details',`${m.id} · Public impact: ${def.public}`,`<form class="ap-form" id="v42PersonForm"><div class="v42-profile-preview"><img src="${esc(m.src||m.thumb)}" alt="${esc(p.name||m.name||m.id)}"><div><strong>${esc(p.name||'Profile details not completed')}</strong><span>${esc(m.id)} · Full source preview</span><span>${esc(def.public)}</span><span>Status: ${esc(completeness(p))}</span></div></div><div class="ap-row"><div class="ap-field"><label>Name</label><input name="name" required value="${esc(p.name)}"></div><div class="ap-field"><label>City</label><input name="city" value="${esc(p.city)}"></div></div><div class="ap-field"><label>Person type / role</label><select name="type">${['Brand Ambassador','Brand Promoter','Community'].map(x=>`<option ${x===p.type?'selected':''}>${x}</option>`).join('')}</select></div><div class="ap-field"><label>Short About / intro</label><textarea name="intro" maxlength="600">${esc(p.intro)}</textarea></div><div class="ap-field"><label>Story / quote</label><textarea name="story" maxlength="1400">${esc(p.story)}</textarea></div><div class="ap-field"><label>Feedback / testimonial</label><textarea name="feedback" maxlength="900">${esc(p.feedback)}</textarea></div><div class="ap-row"><div class="ap-field"><label>Rating (optional)</label><select name="rating"><option value="">No rating</option>${[1,2,3,4,5].map(x=>`<option value="${x}" ${String(x)===String(p.rating)?'selected':''}>${x} / 5</option>`).join('')}</select></div><div class="ap-field"><label>Profile text colour</label><div class="v42-colour-control"><input type="color" name="textColor" value="${esc(textColor)}" aria-label="Profile text colour"><button type="button" class="ap-btn mini" data-v42-colour-reset>Brand dark</button></div><small>Choose the public profile text colour. Low-contrast colours are blocked automatically.</small><div id="v42TextColorSample" class="v42-colour-sample" style="color:${esc(textColor)}">Aa · ${esc(p.name||'Profile text preview')}</div></div></div><div class="ap-field"><label>Public profile</label><label><input type="checkbox" name="public" ${p.public!==false?'checked':''}> Allow these Admin-authored details when a visitor opens this person</label></div><div class="ap-field"><label>Related approved People photos</label><div class="v42-related">${approved.map(x=>`<label><input type="checkbox" name="related" value="${esc(x.id)}" ${(p.relatedMediaIds||[]).includes(x.id)?'checked':''}> ${esc(x.id)} · ${esc(x.name||'People media')}</label>`).join('')}</div></div><div class="v42-impact"><strong>Public impact</strong><span>${esc(def.public)}. Name, city, role, About, story/quote, testimonial, rating, text colour and approved related photos are the public profile source. Text changes never approve a photo. Only Approved + visible + website-allowed People photos can appear publicly.</span></div><div class="ap-v42-actions"><button type="button" class="ap-btn" id="v42PreviewProfile">Preview public profile</button><button class="ap-btn primary" id="v42SaveProfile">${production()?'Save & publish person details':'Save person details'}</button></div><div class="ap-version-note" id="v42ProfileSaveState" hidden></div></form>`);
 const f=$('#v42PersonForm'),colour=f.elements.textColor,sample=$('#v42TextColorSample');
 const syncColour=()=>{const h=hex(colour.value)||DEFAULT_TEXT,safe=safeTextColor(h);sample.style.color=safe;sample.textContent=`Aa · ${f.elements.name.value||'Profile text preview'}${safe!==h?' · low contrast blocked':''}`;};
 colour.addEventListener('input',syncColour);f.elements.name.addEventListener('input',syncColour);$('[data-v42-colour-reset]')?.addEventListener('click',()=>{colour.value=DEFAULT_TEXT;syncColour();});
 $('#v42PreviewProfile').onclick=()=>{syncColour();const d=Object.fromEntries(new FormData(f).entries());toast(`Preview: ${d.name||'Name missing'} · ${d.city||'City not set'} · text ${safeTextColor(d.textColor)}`);};
 f.onsubmit=async e=>{e.preventDefault();const fd=new FormData(f),rawColour=hex(fd.get('textColor'));if(!rawColour||contrast(rawColour,PROFILE_BG)<4.5){colour.focus();sample.textContent='Choose a darker text colour. Minimum contrast is 4.5:1.';return;}const previousIds=new Set(p.relatedMediaIds||[]),nextIds=[...new Set([m.id,...fd.getAll('related')])],submit=$('#v42SaveProfile'),status=$('#v42ProfileSaveState');Object.assign(p,{name:String(fd.get('name')||'').trim(),city:String(fd.get('city')||'').trim(),type:fd.get('type'),intro:String(fd.get('intro')||'').trim(),story:String(fd.get('story')||'').trim(),feedback:String(fd.get('feedback')||'').trim(),rating:fd.get('rating'),public:fd.get('public')==='on',textColor:rawColour,relatedMediaIds:nextIds,updatedAt:new Date().toISOString(),updatedBy:$('#apUserName')?.textContent||role()});previousIds.forEach(id=>{if(nextIds.includes(id))return;const mm=(s.media||[]).find(x=>x.id===id);if(mm&&mm.personId===p.personId)delete mm.personId;});nextIds.forEach(id=>{const mm=(s.media||[]).find(x=>x.id===id);if(mm&&eligible(mm))mm.personId=p.personId;});save(s,'Person profile updated',p.personId);if(!production()){close();toast('Person details saved locally. Storefront profile preview updated.');render();return;}if(submit)submit.disabled=true;if(status){status.hidden=false;status.innerHTML='<strong>Publishing secure People profile…</strong><span>Writing source configuration, then public projection.</span>';}try{await publishProfiles(s);if(status)status.innerHTML='<strong>Published.</strong><span>The public People profile now uses these Admin-authored details.</span>';toast('Person details published. Storefront profile projection updated.');setTimeout(()=>{close();render();},450);}catch(err){console.error('[People profile publish]',err);if(submit)submit.disabled=false;if(status){status.hidden=false;status.innerHTML=`<strong>Public publish failed.</strong><span>${esc(err?.message||err)}. The UI will not bypass Firebase security.</span>`;}toast('Person details not published: '+String(err?.message||err));}};
}
function pickerCard(s,m){const used=streamUse(s,m.id),p=m.personId?s.peopleProfiles?.[m.personId]:null,blocked=used.length>0;return `<button type="button" class="v42-media-card ${blocked?'is-used':''}" data-v42-media-card="${esc(m.id)}" data-search="${esc((m.id+' '+(m.name||'')+' '+(p?.name||'')).toLowerCase())}" ${blocked?'disabled':''}><span class="v42-media-thumb"><img src="${esc(m.thumb||m.src)}" alt="${esc(p?.name||m.name||m.id)}"></span><span class="v42-media-card-copy"><strong>${esc(p?.name||m.name||'People media')}</strong><small>${esc(m.id)}</small><small>${esc(blocked?'Already used: '+used.join(', '):'Available for this rail')}</small><small>${esc(p?.type||'Profile not completed')}</small></span></button>`;}
function openMediaPicker(){if(!canManage())return toast(role()+' cannot manage People streams.');const s=state();ensureStreams(s);const approved=(s.media||[]).filter(eligible);modal('Choose approved People image','Select visually — full source preview, Media ID and current usage are shown before anything is added.',`<div class="v42-picker"><div class="v42-picker-tools"><input id="v42PickerSearch" class="ap-filter" placeholder="Search by person, Media ID or title"><span>${approved.length} approved People images</span></div><div class="v42-picker-layout"><div class="v42-picker-grid">${approved.map(m=>pickerCard(s,m)).join('')||'<div class="ap-empty">No approved People images are available.</div>'}</div><aside class="v42-picker-preview" id="v42PickerPreview"><div class="ap-empty">Select an image to inspect it at a larger size before adding.</div></aside></div><div class="ap-v42-actions v42-picker-actions"><button type="button" class="ap-btn" data-v42-picker-cancel>Cancel</button><button type="button" class="ap-btn primary" data-v42-picker-confirm disabled>Add selected image</button></div></div>`);
 let selected='';const search=$('#v42PickerSearch'),confirm=$('[data-v42-picker-confirm]'),preview=$('#v42PickerPreview');
 function select(id){const m=(s.media||[]).find(x=>x.id===id);if(!m)return;selected=id;$$('[data-v42-media-card]').forEach(c=>c.classList.toggle('is-selected',c.dataset.v42MediaCard===id));const uses=streamUse(s,id);preview.innerHTML=`<div class="v42-picker-large"><img src="${esc(m.src||m.thumb)}" alt="${esc(m.name||m.id)}"></div><strong>${esc(m.name||'People media')}</strong><span>Media ID: ${esc(m.id)}</span><span>${esc(uses.length?'Current use: '+uses.join(', '):'Not currently used in a People rail')}</span><span>${esc(m.personId?'Linked person: '+(s.peopleProfiles?.[m.personId]?.name||m.personId):'No person profile yet')}</span>`;confirm.disabled=uses.length>0;}
 $$('[data-v42-media-card]').forEach(c=>c.addEventListener('click',()=>select(c.dataset.v42MediaCard)));
 search?.addEventListener('input',()=>{const q=search.value.trim().toLowerCase();$$('[data-v42-media-card]').forEach(c=>c.hidden=!!q&&!c.dataset.search.includes(q));});
 $('[data-v42-picker-cancel]')?.addEventListener('click',close);
 confirm?.addEventListener('click',()=>{if(!selected)return;const uses=streamUse(s,selected);if(uses.length)return toast('Remove this image from its existing People rail before reusing it.');const st=s.peopleStreams[active];st.selectedIds=[...new Set([...(st.selectedIds||[]),selected])];save(s,'People stream image added',active+' → '+selected);close();render();});
}
function style(){if($('#apV42PeopleStyle'))return;const x=document.createElement('style');x.id='apV42PeopleStyle';x.textContent=`#apV42PeopleStreams{margin-bottom:16px}.ap-v42-tabs,.ap-v42-actions{display:flex;gap:7px;flex-wrap:wrap}.ap-v42-grid{display:grid;grid-template-columns:minmax(250px,.6fr) minmax(0,1.4fr);gap:16px}.ap-v42-list{display:grid;gap:10px;min-width:0}.ap-v42-row{display:grid;grid-template-columns:minmax(120px,150px) minmax(180px,1fr) minmax(190px,auto);gap:14px;align-items:center;padding:12px;border:1px solid rgba(255,255,255,.1);min-width:0;overflow:hidden}.ap-v42-photo{height:150px;width:100%;display:grid;place-items:center;background:#061006;border-radius:8px;overflow:hidden}.ap-v42-photo img{width:100%;height:100%;object-fit:contain;object-position:center}.ap-v42-meta{display:grid;gap:5px;min-width:0}.ap-v42-meta strong,.ap-v42-meta span{overflow-wrap:anywhere}.ap-v42-meta span{font-size:10px;opacity:.72}.ap-v42-status{font-size:10px}.ap-v42-help{font-size:10px;line-height:1.55;opacity:.65;margin:8px 0}.v42-profile-preview{display:grid;grid-template-columns:150px 1fr;gap:14px;align-items:center;margin-bottom:14px}.v42-profile-preview img{width:150px;height:170px;object-fit:contain;background:#061006;border-radius:10px}.v42-profile-preview div{display:grid;gap:5px}.v42-related{max-height:180px;overflow:auto;display:grid;gap:6px;padding:8px;border:1px solid #ddd;border-radius:8px}.v42-impact{display:grid;gap:4px;padding:10px;background:#f7f3e8;border-radius:10px;margin:10px 0}.v42-colour-control{display:flex;gap:8px;align-items:center}.v42-colour-control input[type=color]{width:58px;height:38px;padding:2px;border:1px solid #cfd7ce;border-radius:8px;background:#fff}.v42-colour-sample{margin-top:7px;padding:9px 10px;border:1px solid #e3dfd2;background:${PROFILE_BG};border-radius:8px;font-size:16px;font-weight:700}.v42-picker-tools{display:flex;gap:12px;align-items:center;justify-content:space-between;margin-bottom:12px}.v42-picker-tools input{min-width:280px}.v42-picker-layout{display:grid;grid-template-columns:minmax(0,1.45fr) minmax(260px,.55fr);gap:16px;min-height:420px}.v42-picker-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;align-content:start;max-height:58vh;overflow:auto;padding:2px}.v42-media-card{appearance:none;border:1px solid #d9ded6;background:#fff;border-radius:12px;padding:8px;text-align:left;display:grid;gap:7px;cursor:pointer;color:#1b2d20;min-width:0}.v42-media-card:hover{border-color:#77957e;box-shadow:0 6px 18px rgba(15,45,25,.08)}.v42-media-card.is-selected{border:2px solid #1e5a34;padding:7px;box-shadow:0 0 0 3px rgba(30,90,52,.12)}.v42-media-card.is-used{opacity:.48;cursor:not-allowed}.v42-media-thumb{height:150px;display:grid;place-items:center;background:#071208;border-radius:8px;overflow:hidden}.v42-media-thumb img{width:100%;height:100%;object-fit:contain;object-position:center}.v42-media-card-copy{display:grid;gap:2px;min-width:0}.v42-media-card-copy strong,.v42-media-card-copy small{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.v42-media-card-copy small{font-size:10px;opacity:.72}.v42-picker-preview{border:1px solid #d9ded6;border-radius:12px;padding:12px;background:#f7f8f5;display:grid;align-content:start;gap:7px;min-width:0}.v42-picker-large{height:330px;background:#061006;border-radius:10px;display:grid;place-items:center;overflow:hidden}.v42-picker-large img{width:100%;height:100%;object-fit:contain;object-position:center}.v42-picker-preview span{font-size:11px;color:#5d675f}.v42-picker-actions{justify-content:flex-end;margin-top:14px}@media(max-width:1100px){.v42-picker-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.v42-picker-layout{grid-template-columns:1fr}.v42-picker-large{height:280px}}@media(max-width:980px){.ap-v42-grid{grid-template-columns:1fr}.ap-v42-row{grid-template-columns:120px minmax(0,1fr)}.ap-v42-row>.ap-v42-actions{grid-column:1/-1}}@media(max-width:620px){.ap-v42-row{grid-template-columns:1fr}.ap-v42-photo{height:180px}.ap-v42-row>.ap-v42-actions{grid-column:auto;display:grid;grid-template-columns:1fr 1fr}.ap-v42-row>.ap-v42-actions .primary{grid-column:1/-1}.v42-profile-preview{grid-template-columns:1fr}.v42-profile-preview img{width:100%;height:220px}.v42-picker-tools{display:grid}.v42-picker-tools input{min-width:0;width:100%}.v42-picker-grid{grid-template-columns:1fr}.v42-media-thumb{height:210px}}`;document.head.appendChild(x);}
function host(){const v=$('.ap-view[data-view="ambassadors"]');if(!v)return null;let h=$('#apV42PeopleStreams');if(!h){h=document.createElement('section');h.id='apV42PeopleStreams';h.className='ap-card ap-panel';v.insertBefore(h,v.firstChild);}return h;}
function render(){style();const h=host();if(!h)return;const s=state();ensureStreams(s);const def=STREAMS.find(x=>x.key===active)||STREAMS[0],st=s.peopleStreams[def.key],ids=(st.selectedIds||[]).filter(id=>eligible(Store.mediaById(s,id)));h.innerHTML=`<div class="ap-panel-head"><div><h2>People Stories · public sequencing & profiles</h2><span>Choose visually, then sequence. Full-source Admin previews prevent selecting the wrong portrait.</span></div><span class="ap-status gold">${esc(role())}</span></div><div class="ap-v42-tabs">${STREAMS.map(x=>`<button class="ap-btn mini" data-v42-stream="${x.key}" aria-selected="${x.key===active}">${esc(x.name)}</button>`).join('')}</div><p class="ap-v42-help">Public impact: ${esc(def.public)} · Person details are authored here and appear when a visitor opens that person from the storefront/People page.</p><div class="ap-v42-grid"><div><div class="ap-field"><span>Add approved People image</span><button class="ap-btn primary" type="button" data-v42-add data-v42-picker-open>Choose approved People image</button><small>Opens a visual picker with thumbnails, Media IDs, profile names and current rail usage.</small></div><div class="ap-v42-actions" style="margin-top:12px"><button class="ap-btn" data-v42-open-review>Open Review Queue</button><button class="ap-btn primary" data-v42-save>Save ${esc(def.name)}</button></div></div><div class="ap-v42-list">${ids.map((id,i)=>{const m=Store.mediaById(s,id),p=profile(s,m,def,false),status=completeness(p);return `<article class="ap-v42-row" data-v42-person-row="${esc(id)}"><div class="ap-v42-photo"><img src="${esc(m.src||m.thumb)}" alt="${esc(p?.name||m.name||id)}"></div><div class="ap-v42-meta"><strong>${String(i+1).padStart(2,'0')} · ${esc(p?.name||m.name||'Person details missing')}</strong><span>Media ID: ${esc(id)}</span><span>City: ${esc(p?.city||'Not added')}</span><span>Role: ${esc(p?.type||def.type)}</span><span>Public: ${esc(def.public)}</span><span>Text colour: ${esc(safeTextColor(p?.textColor))}</span><span class="ap-v42-status">Profile: ${esc(status)}</span></div><div class="ap-v42-actions"><button class="ap-btn mini primary" data-v42-profile="${esc(id)}">Edit person details</button><button class="ap-btn mini" data-v42-up="${esc(id)}" ${i===0?'disabled':''}>↑</button><button class="ap-btn mini" data-v42-down="${esc(id)}" ${i===ids.length-1?'disabled':''}>↓</button><button class="ap-btn mini danger" data-v42-remove="${esc(id)}">Remove</button></div></article>`}).join('')||'<div class="ap-empty">No approved images in this rail.</div>'}</div></div>`;}
function withStream(fn){const s=state();ensureStreams(s);fn(s,s.peopleStreams[active]);return s;}
document.addEventListener('click',e=>{const tab=e.target.closest?.('[data-v42-stream]');if(tab){active=tab.dataset.v42Stream;return render();}const prof=e.target.closest?.('[data-v42-profile]');if(prof)return editPerson(prof.dataset.v42Profile,active);if(e.target.closest?.('[data-v42-picker-open],[data-v42-add]'))return openMediaPicker();if(e.target.closest?.('[data-v42-open-review]')){window.NSV421ReviewQueue?.open?.();if(!window.NSV421ReviewQueue)toast('Review Queue is still loading.');return;}if(!canManage())return;const move=e.target.closest?.('[data-v42-up],[data-v42-down]');if(move){const id=move.dataset.v42Up||move.dataset.v42Down,next=withStream((s,st)=>{const a=st.selectedIds||[],i=a.indexOf(id),j=i+(move.hasAttribute('data-v42-up')?-1:1);if(i>=0&&j>=0&&j<a.length)[a[i],a[j]]=[a[j],a[i]];});save(next,'People stream sequence changed',active+' → '+id);return render();}const rem=e.target.closest?.('[data-v42-remove]');if(rem){const id=rem.dataset.v42Remove,next=withStream((s,st)=>{st.selectedIds=(st.selectedIds||[]).filter(x=>x!==id);});save(next,'People stream image unpublished',active+' → '+id);return render();}if(e.target.closest?.('[data-v42-save]')){const s=state();save(s,'People stream published',active);toast('People sequence saved.');render();}},true);
window.addEventListener('nsv421:change',()=>setTimeout(render,20));
window.addEventListener('nsv421:production-ready',()=>{loadRemoteProfiles().finally(()=>setTimeout(render,30));});
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',()=>{render();setTimeout(loadRemoteProfiles,80);},{once:true}):(render(),setTimeout(loadRemoteProfiles,80));
window.NSV421PeopleAdmin={ready:true,canManage,publishProfiles,loadRemoteProfiles,editPerson,openMediaPicker,safeTextColor};
})();
