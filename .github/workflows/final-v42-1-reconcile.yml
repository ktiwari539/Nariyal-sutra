name: Final V42.1 reconcile QA

on:
  push:
    branches:
      - fix/final-v42-1-reconciled-20260911
  workflow_dispatch:

permissions:
  contents: read

jobs:
  qa:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout reconciled branch
        uses: actions/checkout@v4
      - name: Run static pre-deploy audit
        run: node scripts/predeploy-audit.mjs
      - name: Install browser QA dependencies
        shell: bash
        run: |
          npm install --no-save --no-package-lock playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Run browser and user-experience QA
        shell: bash
        run: |
          python3 -m http.server 4173 --bind 127.0.0.1 > /tmp/nariyal-http.log 2>&1 &
          SERVER_PID=$!
          trap 'kill $SERVER_PID || true' EXIT
          sleep 2
          node scripts/final-release-regression-qa.mjs
          node scripts/visual-qa.mjs
          node scripts/content-integrity-qa.mjs
          node scripts/user-experience-qa.mjs
      - name: Upload final QA evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nariyal-v42-1-final-reconcile-qa
          path: qa-artifacts/
          if-no-files-found: warn
          retention-days: 7
