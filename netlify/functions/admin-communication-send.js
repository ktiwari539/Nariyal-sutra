'use strict';
const crypto=require('crypto');
const {completeTemplateParams}=require('./email-template-contract');
const {verifyAppCheckRequest}=require('./app-check-verify');
let certCache={expiresAt:0,certs:null};
const buckets=new Map();
function allowedOrigin(origin){if(!origin)return null;try{const u=new URL(origin);if(u.protocol!=='https:')return null;if(u.hostname==='nariyal-sutra.netlify.app'||/--nariyal-sutra\.netlify\.app$/i.test(u.hostname)||(process.env.URL&&u.origin===new URL(process.env.URL).origin))return origin;}catch(_){}return null;}
function json(statusCode,body,origin){return{statusCode,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store','access-control-allow-origin':origin||'https://nariyal-sutra.netlify.app','access-control-allow-methods':'POST, OPTIONS','access-control-allow-headers':'content-type, authorization, x-firebase-appcheck','vary':'Origin'},body:statusCode===204?'':JSON.stringify(body)};}
function b64url(input){let s=String(input||'').replace(/-/g,'+').replace(/_/g,'/');while(s.length%4)s+='=';return Buffer.from(s,'base64');}
async function googleCerts(){if(certCache.certs&&Date.now()<certCache.expiresAt)return certCache.certs;const r=await fetch('https://www.googleapis.com/robot/v1/metadata/x509/securetoken@system.gserviceaccount.com');if(!r.ok)throw new Error('Firebase certificate lookup failed');const certs=await r.json(),cc=r.headers.get('cache-control')||'',maxAge=Number((cc.match(/max-age=(\d+)/)||[])[1]||1800);certCache={certs,expiresAt:Date.now()+Math.max(300,maxAge-60)*1000};return certs;}
async function verifyFirebaseToken(token,projectId){const parts=String(token||'').split('.');if(parts.length!==3)throw new Error('Invalid Firebase token');let header,payload;try{header=JSON.parse(b64url(parts[0]).toString('utf8'));payload=JSON.parse(b64url(parts[1]).toString('utf8'));}catch(_){throw new Error('Invalid Firebase token');}if(header.alg!=='RS256'||!header.kid)throw new Error('Invalid Firebase token algorithm');const certs=await googleCerts(),cert=certs[header.kid];if(!cert)throw new Error('Unknown Firebase signing key');const ok=crypto.verify('RSA-SHA256',Buffer.from(parts[0]+'.'+parts[1]),cert,b64url(parts[2]));if(!ok)throw new Error('Firebase token signature rejected');const now=Math.floor(Date.now()/1000);if(payload.aud!==projectId||payload.iss!==`https://securetoken.google.com/${projectId}`)throw new Error('Firebase token audience rejected');if(!payload.sub||payload.sub.length>128||Number(payload.exp||0)<=now||Number(payload.iat||0)>now+60)throw new Error('Firebase token expired or invalid');if(Number(payload.auth_time||0)>now+60)throw new Error('Firebase token auth time invalid');return payload;}
function field(doc,key,type){return doc?.fields?.[key]?.[type];}
function firebaseHeaders(token,appCheckToken){const headers={authorization:`Bearer ${token}`};if(appCheckToken)headers['X-Firebase-AppCheck']=appCheckToken;return headers;}
async function adminRole(claims,token,appCheckToken,projectId,ownerUid,ownerEmail){const uid=String(claims.sub||''),email=String(claims.email||'');if(claims.email_verified!==true||!email)return'';if(uid===ownerUid)return email===ownerEmail?'Owner':'';const url=`https://firestore.googleapis.com/v1/projects/${encodeURIComponent(projectId)}/databases/(default)/documents/adminRoles/${encodeURIComponent(uid)}`;const r=await fetch(url,{headers:firebaseHeaders(token,appCheckToken)});if(!r.ok)return'';const doc=await r.json(),role=String(field(doc,'role','stringValue')||''),status=String(field(doc,'status','stringValue')||''),active=field(doc,'active','booleanValue')===true,roleEmail=String(field(doc,'email','stringValue')||'');if(status!=='Active'||active!==true||roleEmail!==email)return'';return['Admin','Manager','Operations','Content','Support','Sales'].includes(role)?role:'';}
function rateOk(uid,ip){const now=Date.now(),key=String(uid||'')+'|'+String(ip||'unknown'),b=(buckets.get(key)||[]).filter(x=>now-x<60000);if(b.length>=6)return false;b.push(now);buckets.set(key,b);return true;}
function clean(v,max){return String(v??'').replace(/[\r\0]/g,' ').trim().slice(0,max);}
function validEmail(v){return/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v);}
exports.handler=async(event)=>{
 const origin=allowedOrigin(event.headers?.origin||event.headers?.Origin);
 if(event.httpMethod==='OPTIONS')return origin?json(204,{},origin):json(403,{error:'Origin not allowed'});
 if(event.httpMethod!=='POST')return json(405,{error:'Method not allowed'},origin||undefined);
 if(!origin)return json(403,{error:'Origin not allowed'});
 const ct=String(event.headers?.['content-type']||event.headers?.['Content-Type']||'');if(!ct.includes('application/json'))return json(415,{error:'JSON content type required'},origin);
 if(Number(event.headers?.['content-length']||0)>16384||String(event.body||'').length>16384)return json(413,{error:'Request too large'},origin);
 let appCheck;try{appCheck=await verifyAppCheckRequest(event);}catch(_){return json(401,{error:'App Check verification failed.'},origin);}
 const projectId=process.env.FIREBASE_PROJECT_ID||'nariyal-sutra',ownerUid=process.env.NS_ADMIN_OWNER_UID||'9FjkrCMDstfVS1Ghu2LlA0skoAf2',ownerEmail=process.env.NS_OWNER_EMAIL||'nariyalsutra@gmail.com';
 const authHeader=String(event.headers?.authorization||event.headers?.Authorization||''),token=authHeader.match(/^Bearer\s+(.+)$/i)?.[1]||'',appCheckToken=appCheck.token;if(!token)return json(401,{error:'Admin authentication required.'},origin);
 let claims;try{claims=await verifyFirebaseToken(token,projectId);}catch(_){return json(401,{error:'Admin authentication could not be verified.'},origin);}
 const role=await adminRole(claims,token,appCheckToken,projectId,ownerUid,ownerEmail);if(!['Owner','Admin','Manager'].includes(role))return json(403,{error:'This active verified Admin role cannot send external email communications.'},origin);
 const ip=(event.headers?.['x-nf-client-connection-ip']||event.headers?.['x-forwarded-for']||'').split(',')[0].trim();if(!rateOk(claims.sub,ip))return json(429,{error:'Too many communication send attempts. Try again shortly.'},origin);
 let body={};try{body=JSON.parse(event.body||'{}');}catch(_){return json(400,{error:'Invalid JSON body'},origin);}
 const communicationId=clean(body.communicationId,80),to=clean(body.to,180),recipient=clean(body.recipient,160),purpose=clean(body.purpose,120),orderId=clean(body.orderId,80),subject=clean(body.subject,200),message=clean(body.message,2000);
 if(!communicationId)return json(400,{error:'Communication ID is required.'},origin);if(!validEmail(to))return json(400,{error:'A valid recipient email is required.'},origin);if(!subject)return json(400,{error:'Email subject is required.'},origin);if(!message)return json(400,{error:'Email message is required.'},origin);
 const privateKey=process.env.EMAILJS_PRIVATE_KEY,publicKey=process.env.EMAILJS_PUBLIC_KEY||'aDjTgmhSPrBeTpnOA',serviceId=process.env.EMAILJS_SERVICE_ID||'service_c24xpf8',templateId=process.env.EMAILJS_TEMPLATE_ID||'template_y01paqk',siteUrl=process.env.URL||'https://nariyal-sutra.netlify.app';
 if(!privateKey)return json(503,{error:'Email provider private key is not configured.'},origin);
 const params=completeTemplateParams({from_name:'Nariyal Sutra',reply_to:ownerEmail,to_email:to,email_subject:subject,subject,preheader:subject,headline:subject,status_label:'Approved Admin communication',status_message:message,email_kind:'admin_communication',show_order_details:'',show_tracking:'',detail_title:'Message',order_id:orderId,customer_name:recipient||'Customer',buyer_name:recipient||'Customer',email:to,phone:'',product_name:purpose||'',quantity:'',rate:'',total:'',payment:'',address:'',city:'',notes:message,order_date:new Date().toLocaleString('en-IN',{timeZone:'Asia/Kolkata'}),site_url:siteUrl,admin_url:siteUrl.replace(/\/$/,'')+'/admin',account_url:siteUrl.replace(/\/$/,'')+'/#account',tracking_url:'',cta_text:'Visit Nariyal Sutra',cta_url:siteUrl,status:'approved',status_note:`Communication ${communicationId} sent by ${role}`});
 const payload={service_id:serviceId,template_id:templateId,user_id:publicKey,accessToken:privateKey,template_params:params};
 try{const r=await fetch('https://api.emailjs.com/api/v1.0/email/send',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});const t=await r.text();if(!r.ok)return json(502,{error:'Email provider rejected request.',providerStatus:r.status},origin);return json(200,{ok:true,provider:'emailjs',communicationId,result:t||'OK'},origin);}catch(_){return json(502,{error:'Email provider unavailable.'},origin);}
};
