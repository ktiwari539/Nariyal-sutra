import { chromium } from 'playwright';
import { spawn } from 'node:child_process';
import fs from 'node:fs';
import path from 'node:path';
const BASE='http://127.0.0.1:4203',OUT=path.join(process.cwd(),'qa-artifacts','admin-owner-control');
const ORDER_OPS=path.join(process.cwd(),'assets','js','admin-v44-order-operations.js'),OTP_UI=path.join(process.cwd(),'assets','js','admin-v46-owner-order-otp.js'),PROD_BRIDGE=path.join(process.cwd(),'assets','js','v421-production-bridge.js');fs.mkdirSync(OUT,{recursive:true});
const must=(c,m)=>{if(!c)throw new Error(m)};
must(fs.existsSync(ORDER_OPS),'Order operations module missing');
must(fs.existsSync(OTP_UI),'Owner OTP module missing');
const contract=fs.readFileSync(ORDER_OPS,'utf8');
for(const token of [
  'runTransaction',
  'patch={status:next,updatedAt:now}',
  "if(next==='cancelled')",
  "transitionOrder(orderId,'cancelled'",
  'publicTracking',
  'customerOrders',
  'MANAGE_ROLES',
  'Cancelling a paid order does not automatically issue a refund.',
  'deleteTestOrder',
  'tx.delete(orderRef)',
  'data-ns-order-delete-test',
  'Only the Owner can permanently delete test orders',
  'LOCAL PREVIEW ONLY — production data will not change.',
  'previewCancelOrder',
  'previewDeleteOrder',
  'admin-v46-owner-order-otp.js'
]) must(contract.includes(token),`Order operations contract missing: ${token}`);
const otpContract=fs.readFileSync(OTP_UI,'utf8');
for(const token of ['Delete selected test orders','LOCAL PREVIEW SIMULATION','[data-ns-order-delete-test]','verify_delete'])must(otpContract.includes(token),`Owner OTP contract missing: ${token}`);
const bridgeContract=fs.readFileSync(PROD_BRIDGE,'utf8');
for(const demoId of ['NS-1048','NS-1047','NS-1046','NS-1042'])must(bridgeContract.includes(demoId),`Production bridge demo suppression missing ${demoId}`);
const server=spawn('python3',['-m','http.server','4203','--bind','127.0.0.1'],{stdio:'ignore'});await new Promise(r=>setTimeout(r,900));const browser=await chromium.launch({headless:true});
async function installQaOrder(page){await page.evaluate(()=>{const body=document.querySelector('#apOrdersBody');if(!body)return;body.innerHTML='<tr data-order-qa="1"><td class="mono">QA-ORDER-001</td><td>QA Customer</td><td>Tender Coconut</td><td>12</td><td><span class="ap-status green">Paid</span></td><td>Preparing</td><td>₹660</td></tr>';body.querySelector('[data-order-qa]')?.addEventListener('click',()=>{const detail=document.querySelector('#apOrderDetail');if(detail)detail.innerHTML='<h3>QA-ORDER-001</h3><p>QA Customer</p><div class="ap-divider"></div><div class="ap-detail-grid"><div class="ap-detail"><label>Payment</label><span>Paid</span></div><div class="ap-detail"><label>Delivery</label><span>Preparing</span></div></div>';});});await page.evaluate(()=>window.NSOwnerOrderDeleteOTP?.refresh?.());}
async function checkOrderControls(page,label){const orderNavActivated=await page.evaluate(()=>{const b=document.querySelector('#apNav button[data-view="orders"]');if(!b)return false;b.click();return true;});must(orderNavActivated,`${label}: Orders nav button missing`);await page.waitForFunction(()=>document.querySelector('#apOrdersBody')?.closest('.ap-view')?.classList.contains('is-active'),null,{timeout:5000});await page.waitForTimeout(120);await installQaOrder(page);const first=page.locator('#apOrdersBody tr[data-order-qa]').first();must(await first.count()===1,`${label}: deterministic QA order row missing`);await first.click();await page.waitForTimeout(100);await page.evaluate(()=>window.NSV421OrderOperations?.refresh?.());await page.waitForTimeout(40);const orderId=(await page.locator('#apOrderDetail h3').textContent())?.trim();must(orderId==='QA-ORDER-001',`${label}: selected QA order id missing`);must(await page.locator('#nsOrderOperations .ns-order-preview').count()===1,`${label}: local preview safety notice missing`);must(await page.locator(`[data-ns-order-cancel="${orderId}"]`).count()===1,`${label}: Owner cancel control missing`);must(await page.locator(`[data-ns-order-delete-test="${orderId}"]`).count()===1,`${label}: Owner delete-test control missing`);const cancelConfirm=await page.evaluate(async id=>{let seen='';window.prompt=()=> 'QA cancellation';window.confirm=m=>{seen=String(m);return false;};document.querySelector(`[data-ns-order-cancel="${id}"]`)?.click();await new Promise(r=>setTimeout(r,20));return seen;},orderId);must(cancelConfirm.includes(orderId),`${label}: cancel confirmation omits order id`);must(/retained for history/i.test(cancelConfirm),`${label}: cancel confirmation omits history semantics`);must(/does NOT issue a refund/i.test(cancelConfirm),`${label}: paid cancellation refund warning missing`);must(/LOCAL PREVIEW ONLY/i.test(cancelConfirm),`${label}: local cancellation safety warning missing`);
 must(await page.locator(`[data-ns-order-delete-test="${orderId}"]`).isHidden(),`${label}: destructive action should initially be hidden behind Advanced Owner tools`);await page.locator('#nsOrderOperations .ns-order-admin-tools summary').click();must(await page.locator(`[data-ns-order-delete-test="${orderId}"]`).isVisible(),`${label}: Advanced Owner tools did not reveal test-order deletion`);await page.locator(`[data-ns-order-delete-test="${orderId}"]`).click();await page.waitForSelector('#nsOwnerDeleteOtpModal');const deleteFlow=await page.locator('#nsOwnerDeleteOtpModal').innerText();must(deleteFlow.includes(orderId),`${label}: OTP deletion confirmation omits exact order id`);must(/LOCAL PREVIEW SIMULATION/i.test(deleteFlow),`${label}: local delete OTP simulation warning missing`);must(/never inferred/i.test(deleteFlow),`${label}: delete flow does not state explicit-order semantics`);must(await page.locator('#nsOwnerDeleteOtpModal [data-ns-otp-input]').count()===0,`${label}: local preview rendered a real OTP field`);await page.locator('#nsOwnerDeleteOtpModal [data-ns-otp-close]').click();
 await page.selectOption('#apRole','Operations');await page.waitForTimeout(80);await page.evaluate(()=>window.NSV421OrderOperations?.refresh?.());must(await page.locator(`[data-ns-order-cancel="${orderId}"]`).count()===1,`${label}: Operations cancel control missing`);must(await page.locator('[data-ns-order-delete-test]').count()===0,`${label}: Operations must not see hard delete`);await page.selectOption('#apRole','Support');await page.waitForTimeout(80);await page.evaluate(()=>window.NSV421OrderOperations?.refresh?.());must(await page.locator('[data-ns-order-cancel]').count()===0,`${label}: Support must not see cancel`);must(await page.locator('[data-ns-order-delete-test]').count()===0,`${label}: Support must not see hard delete`);await page.selectOption('#apRole','Owner');await page.waitForTimeout(80);await page.evaluate(()=>window.NSV421OrderOperations?.refresh?.());must(await page.locator(`[data-ns-order-delete-test="${orderId}"]`).count()===1,`${label}: Owner delete-test control did not return`);}
async function openAdmin(width,height,label){const context=await browser.newContext({viewport:{width,height},deviceScaleFactor:width<900?2:1,hasTouch:width<900,serviceWorkers:'block'});const page=await context.newPage(),errors=[];page.on('pageerror',e=>errors.push(String(e)));const r=await page.goto(BASE+'/admin.html',{waitUntil:'domcontentloaded',timeout:30000});must(r&&r.status()<400,`${label}: admin status ${r?.status()}`);await page.waitForFunction(()=>window.__NS_V421_ADMIN_V40__===true&&document.querySelector('#apV40OwnerStatus'),null,{timeout:12000});if(!(await page.evaluate(()=>window.__NS_V421_ORDER_OPERATIONS__===true))){await page.addScriptTag({path:ORDER_OPS});}await page.waitForFunction(()=>window.__NS_V421_ORDER_OPERATIONS__===true&&window.__NS_V421_OWNER_ORDER_OTP__===true,null,{timeout:5000});await page.waitForTimeout(300);const base=await page.evaluate(()=>({role:document.querySelector('#apRole')?.value,sw:document.documentElement.scrollWidth,iw:innerWidth,views:[...document.querySelectorAll('.ap-view.is-active')].length,owner:document.querySelector('#apV40OwnerStatus')?.innerText||''}));must(base.role==='Owner',`${label}: expected Owner preview role, got ${base.role}`);must(base.views===1,`${label}: active views=${base.views}`);must(base.sw<=base.iw+8,`${label}: horizontal overflow ${base.sw}>${base.iw}`);must(/Owner control/i.test(base.owner),`${label}: Owner control status missing`);await checkOrderControls(page,label);if(width<900){await page.locator('#apMenuBtn').click();await page.waitForTimeout(80);}await page.locator('#apNav button[data-view="pages"]').click();await page.waitForTimeout(200);must(await page.locator('#apV38ImageTargets[data-v40-owned="1"]').count()===1,`${label}: V40 storefront placement map missing`);const change=page.locator('[data-v40-target]').first();must(await change.isVisible(),`${label}: Change image not visible`);await change.click();await page.waitForTimeout(100);must(await page.locator('#apModal.is-open .ap-v40-picker').count()===1,`${label}: Change image did not open approved picker`);must(await page.locator('#apV40Impact.is-show').count()===1,`${label}: meaningful impact explanation not shown`);must(await page.locator('#apV40Impact').evaluate(el=>getComputedStyle(el).pointerEvents)==='none',`${label}: impact helper blocks pointer interaction`);await page.locator('#apModalClose').click();if(width<900){await page.locator('#apMenuBtn').click();await page.waitForTimeout(80);}await page.locator('#apNav button[data-view="ambassadors"]').click();await page.waitForTimeout(250);must(await page.locator('#apV42PeopleStreams').count()===1,`${label}: People Stories controller missing`);must(await page.locator('[data-v42-save]').isVisible(),`${label}: People Stories save control missing`);must(await page.locator('#apV40Ambassador').evaluateAll(xs=>xs.every(x=>getComputedStyle(x).display==='none'),`${label}: duplicate legacy Ambassador editor is still visible`),`${label}: duplicate legacy Ambassador editor is still visible`);must(await page.locator('[data-v43-upload-people]').isVisible(),`${label}: People upload guidance missing`);await page.screenshot({path:path.join(OUT,`${label}-ambassadors.png`),fullPage:true});must(!errors.length,`${label}: page errors ${errors.join(' | ')}`);await context.close();}
try{await openAdmin(1440,900,'desktop');await openAdmin(820,1100,'tablet');await openAdmin(390,844,'mobile');console.log('ADMIN OWNER CONTROL QA: PASS');}finally{await browser.close();server.kill();}
