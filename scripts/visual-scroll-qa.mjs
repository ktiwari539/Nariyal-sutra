import { chromium } from 'playwright';
import { spawn } from 'node:child_process';
import fs from 'node:fs';
import path from 'node:path';

const BASE='http://127.0.0.1:4198',OUT=path.join(process.cwd(),'qa-artifacts','visual-scroll'),must=(c,m)=>{if(!c)throw new Error(m)};fs.mkdirSync(OUT,{recursive:true});
const server=spawn('python3',['-m','http.server','4198','--bind','127.0.0.1'],{stdio:'ignore'});await new Promise(r=>setTimeout(r,900));const browser=await chromium.launch({headless:true});
async function openPage(url,label){const context=await browser.newContext({viewport:{width:1440,height:900},serviceWorkers:'block'}),page=await context.newPage(),errors=[];page.on('pageerror',e=>errors.push(String(e)));const res=await page.goto(BASE+url,{waitUntil:'domcontentloaded',timeout:25000});must(res&&res.status()<400,`${label} returned ${res?.status()}`);await page.waitForTimeout(650);must(!errors.length,`${label} page errors: ${errors.join(' | ')}`);return {context,page,errors};}
async function enterHome(page){const usable=await page.evaluate(()=>{const m=document.getElementById('main-site');if(!m)return false;const s=getComputedStyle(m);return Number.parseFloat(s.opacity||'0')>.9&&s.pointerEvents!=='none';});if(!usable){const skip=page.locator('#ji-skip');if(await skip.count()&&await skip.isVisible().catch(()=>false))await skip.click({timeout:3000}).catch(()=>{});}await page.waitForFunction(()=>{const m=document.getElementById('main-site');if(!m)return false;const s=getComputedStyle(m);return Number.parseFloat(s.opacity||'0')>.9&&s.pointerEvents!=='none';},null,{timeout:7000});await page.evaluate(()=>scrollTo(0,0));}
async function scrollThrough(page){const max=await page.evaluate(()=>Math.max(0,document.documentElement.scrollHeight-innerHeight));for(let i=0;i<=10;i++){await page.evaluate(y=>scrollTo(0,y),Math.round(max*i/10));await page.waitForTimeout(75);}await page.evaluate(v=>scrollTo(0,v),max);await page.waitForTimeout(140);}
async function assertSourcesLoad(page,selector,label){const loc=page.locator(selector).first();must(await loc.count()===1,`${label}: missing ${selector}`);const result=await loc.evaluate(async el=>{const srcs=[...new Set([...el.querySelectorAll('img')].map(img=>img.getAttribute('src')||'').filter(Boolean))],probe=src=>new Promise(resolve=>{const img=new Image(),timer=setTimeout(()=>resolve({src,ok:false,timeout:true,w:0,h:0}),3500),done=ok=>{clearTimeout(timer);resolve({src,ok,w:img.naturalWidth||0,h:img.naturalHeight||0});};img.onload=()=>done(img.naturalWidth>0);img.onerror=()=>done(false);img.src=src;});return {count:srcs.length,checks:await Promise.all(srcs.map(probe))};});const failed=result.checks.filter(x=>!x.ok);must(!failed.length,`${label}: configured image sources failed independent decode/load: ${JSON.stringify(failed)}`);return result;}
async function center(page,loc){await loc.scrollIntoViewIfNeeded({timeout:7000});await page.waitForTimeout(180);return loc.evaluate(el=>{const r=el.getBoundingClientRect();return r.right>0&&r.left<innerWidth&&r.bottom>0&&r.top<innerHeight&&r.width>2&&r.height>2;});}
async function visibleShot(page,selector,name){const loc=page.locator(selector).first();must(await loc.count()===1,`${name}: missing ${selector}`);must(await center(page,loc),`${name}: target could not be positioned in viewport`);const state=await loc.evaluate(el=>{const s=getComputedStyle(el),r=el.getBoundingClientRect(),imgs=[...el.querySelectorAll('img')].map(img=>{const ir=img.getBoundingClientRect(),cs=getComputedStyle(img),visible=cs.display!=='none'&&cs.visibility!=='hidden'&&Number.parseFloat(cs.opacity||'1')>.05&&ir.right>0&&ir.left<innerWidth&&ir.bottom>0&&ir.top<innerHeight&&ir.width>2&&ir.height>2;return {src:img.getAttribute('src')||'',visible,ok:img.complete&&img.naturalWidth>0};});return {display:s.display,visibility:s.visibility,opacity:Number.parseFloat(s.opacity||'1'),w:r.width,h:r.height,imgs,visibleImgs:imgs.filter(x=>x.visible)};});must(state.display!=='none'&&state.visibility!=='hidden'&&state.opacity>.05&&state.w>120&&state.h>40,`${name}: target is not visibly rendered ${JSON.stringify(state)}`);if(state.imgs.length){must(state.visibleImgs.length>0,`${name}: no visible images ${JSON.stringify(state)}`);must(state.visibleImgs.every(x=>x.ok),`${name}: visible image failed ${JSON.stringify(state.visibleImgs)}`);}await loc.screenshot({path:path.join(OUT,name),timeout:10000});return state;}
async function exerciseReveals(page,label,minCount){const reveals=page.locator('.w-reveal'),count=await reveals.count();must(count>=minCount,`${label}: expected at least ${minCount} reveal sections, found ${count}`);for(let i=0;i<count;i++){const loc=reveals.nth(i);await loc.scrollIntoViewIfNeeded({timeout:7000});await page.waitForFunction(index=>{const el=document.querySelectorAll('.w-reveal')[index];return !!el&&el.classList.contains('is-visible')&&Number.parseFloat(getComputedStyle(el).opacity||'1')>.05;},i,{timeout:3500});}}
async function noBroken(page,label){const broken=await page.evaluate(()=>[...document.images].filter(img=>{const s=getComputedStyle(img),r=img.getBoundingClientRect();return img.complete&&img.naturalWidth===0&&s.display!=='none'&&s.visibility!=='hidden'&&!img.hidden&&r.width>2&&r.height>2;}).map(img=>img.getAttribute('src')||''));must(!broken.length,`${label} broken images after scroll: ${JSON.stringify(broken)}`);}
try{
 {const {context,page}=await openPage('/','Homepage');await enterHome(page);await page.waitForSelector('#nsPeopleMarquee .ns-face-card',{timeout:10000});await assertSourcesLoad(page,'#nsPeopleMarquee','Homepage People rail');await scrollThrough(page);await visibleShot(page,'#nsPeopleMarquee','homepage-people-reel-visible.png');await visibleShot(page,'.trade-route.trade-supply','homepage-sourcing-visible.png');await visibleShot(page,'.trade-route.trade-buy','homepage-hospitality-visible.png');await noBroken(page,'Homepage');await context.close();}
 {const {context,page}=await openPage('/direct-farm.html','Direct sourcing');await page.waitForFunction(()=>document.documentElement.dataset.nsMediaIntegrity==='ready',null,{timeout:10000});await exerciseReveals(page,'Direct sourcing',6);await visibleShot(page,'.brand-note-grid','direct-sourcing-brand-note.png');await visibleShot(page,'.source-proof-grid','direct-sourcing-proof.png');await visibleShot(page,'.farm-step:nth-child(1)','direct-sourcing-chain.png');await noBroken(page,'Direct sourcing');await context.close();}
 {const {context,page}=await openPage('/supplier-partnership.html','Supplier');await page.waitForFunction(()=>document.documentElement.dataset.nsMediaIntegrity==='ready',null,{timeout:10000});await exerciseReveals(page,'Supplier',5);await visibleShot(page,'.supplier-photo','supplier-photo-visible.png');await visibleShot(page,'.supplier-questions','supplier-questions-visible.png');await noBroken(page,'Supplier');await context.close();}
 {const {context,page}=await openPage('/people-of-nariyal-sutra.html','People page');await page.waitForSelector('#v25PeopleStreams [data-stream="ambassadors"]',{timeout:10000});await assertSourcesLoad(page,'#v25PeopleStreams [data-stream="ambassadors"]','People Ambassador stream');await scrollThrough(page);await visibleShot(page,'#v25PeopleStreams [data-stream="ambassadors"]','people-ambassadors-visible.png');await noBroken(page,'People page');await context.close();}
 for(const item of [{url:'/freshness-first.html',label:'Freshness',selector:'.water-window',name:'freshness-motion-visible.png'},{url:'/fresh-tender-coconut.html',label:'Fresh tender',selector:'.sp-video',name:'fresh-tender-motion-visible.png'},{url:'/green-coconut.html',label:'Green coconut',selector:'.sp-video',name:'green-coconut-motion-visible.png'},{url:'/coconut-events-hospitality.html',label:'Hospitality',selector:'.sp-video',name:'hospitality-motion-visible.png'}]){const {context,page}=await openPage(item.url,item.label);await page.waitForFunction(()=>document.documentElement.dataset.nsMediaIntegrity==='ready',null,{timeout:10000});await assertSourcesLoad(page,item.selector,`${item.label} motion/still`);await scrollThrough(page);const state=await visibleShot(page,item.selector,item.name);must(state.visibleImgs.some(x=>x.ok),`${item.label}: motion/still visual did not render a valid visible image`);await noBroken(page,item.label);await context.close();}
 console.log('VISUAL SCROLL + REVEAL QA: PASS');
} finally {await browser.close();server.kill();}
