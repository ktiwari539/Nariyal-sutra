import { chromium } from 'playwright';
import { spawn } from 'node:child_process';
import fs from 'node:fs';
import path from 'node:path';

const ROOT=process.cwd(),OUT=path.join(ROOT,'qa-artifacts','release-candidate'),BASE='http://127.0.0.1:4187';
fs.mkdirSync(OUT,{recursive:true});
const server=spawn('python3',['-m','http.server','4187','--bind','127.0.0.1'],{stdio:'ignore'});const sleep=ms=>new Promise(r=>setTimeout(r,ms));await sleep(900);
const failures=[],fail=(scope,msg)=>failures.push({scope,msg});
function sourceContract(){
 const read=p=>fs.readFileSync(path.join(ROOT,p),'utf8'),redirects=read('_redirects'),adminEntry=read('admin.html'),adminPreview=read('admin-preview.html'),mediaDb=read('assets/js/v421-media-db.js'),bridge=read('assets/js/v421-production-bridge.js'),v32=read('assets/js/v32-public.js'),v29=read('assets/js/v29-public.js');
 for(const token of ['/admin /admin.html 200','/admin/ /admin.html 200'])if(!redirects.includes(token))fail('source',`missing Business Command Center route: ${token}`);
 if(/\/admin\/?\s+\/admin-live-legacy\.html\s+200/.test(redirects))fail('source','/admin still routes to rejected legacy Admin');
 if(!adminEntry.includes('admin-preview.html')||adminEntry.includes('admin-live-legacy.html'))fail('source','admin.html is not a clean Business Command Center entry');
 if(!adminPreview.includes('assets/js/v421-media-db.js'))fail('source','Business Command Center does not load media/runtime bootstrap');
 if(!mediaDb.includes('admin-v38-production.js')||!mediaDb.includes('v421-production-bridge.js'))fail('source','Business Command Center production scripts are not loaded');
 for(const token of ['admin-bcc-login.html','waitAuth()','adminRole(user)',"const allowed=['Owner',...STAFF_ROLES]",'nsProductionAdminGuard','uploadMedia','persistRemote'])if(!bridge.includes(token))fail('source',`production Admin bridge missing ${token}`);
 const staffRoleSource=bridge.match(/const STAFF_ROLES=\[([^\]]+)\]/)?.[1]||'',staffRoles=[...staffRoleSource.matchAll(/'([^']+)'/g)].map(x=>x[1]),effectiveRoles=['Owner',...staffRoles],expectedRoles=['Owner','Admin','Manager','Operations','Content','Support','Sales'];
 if(JSON.stringify(effectiveRoles)!==JSON.stringify(expectedRoles))fail('source',`production Admin effective role contract changed: ${JSON.stringify(effectiveRoles)}`);
 if(!bridge.includes("const LOCAL=/^(localhost|127\\.0\\.0\\.1|\\[::1\\])$/i"))fail('source','production bridge local/production boundary is missing');
 if(!bridge.includes("const isAdmin=/admin-preview\\.html$/i"))fail('source','production bridge is not bound to approved Business Command Center');
 if(!v32.includes("cinematic:()=>$('#v421-cinematic-film-pro')"))fail('source','homepage sequencing does not target the approved professional cinematic');
 if(!/function loadProfessionalCinematic/.test(v29))fail('source','professional cinematic loader is missing');
}
sourceContract();
async function enter(page){await page.waitForTimeout(550);const skip=page.locator('#ji-skip');if(await skip.count()&&await skip.isVisible().catch(()=>false))await skip.click().catch(()=>{});await page.waitForFunction(()=>{const m=document.getElementById('main-site');if(!m)return false;const s=getComputedStyle(m);return parseFloat(s.opacity||0)>.9&&s.visibility!=='hidden'&&s.pointerEvents!=='none';},null,{timeout:8000});await page.evaluate(()=>{document.documentElement.style.scrollBehavior='auto';scrollTo(0,0);});}
const browser=await chromium.launch({headless:true});
try{
 for(const vp of [{label:'desktop',width:1440,height:900},{label:'tablet',width:820,height:1100},{label:'mobile',width:390,height:844}]){
  const context=await browser.newContext({viewport:{width:vp.width,height:vp.height},serviceWorkers:'block',reducedMotion:'no-preference'}),page=await context.newPage(),errors=[];page.on('pageerror',e=>errors.push(String(e)));
  const response=await page.goto(BASE+'/',{waitUntil:'domcontentloaded',timeout:30000}).catch(()=>null);if(!response||response.status()>=400){fail(vp.label,`storefront returned ${response?.status()||'no response'}`);await context.close();continue;}
  try{await enter(page);}catch(e){fail(vp.label,'storefront entry failed: '+e);}
  await page.waitForSelector('#v421-cinematic-film-pro[data-ready="1"]',{timeout:20000}).catch(e=>fail(vp.label,`professional cinematic missing: ${e}`));await page.waitForTimeout(450);
  const first=await page.evaluate(()=>{const visible=e=>{if(!e)return false;const s=getComputedStyle(e),r=e.getBoundingClientRect();return s.display!=='none'&&s.visibility!=='hidden'&&parseFloat(s.opacity||1)>.01&&r.width>2&&r.height>2},pathOf=sel=>{const i=document.querySelector(sel);return i?new URL(i.currentSrc||i.src,location.href).pathname:''};const productImgs=[...document.querySelectorAll('[data-product-card] .pc-img-wrap img')],motion=document.getElementById('live-motion'),harvest=document.querySelector('#harvest-film .harvest-scene'),pro=document.getElementById('v421-cinematic-film-pro'),legacy=['scroll-cinema','v421-cinematic-film'].map(id=>document.getElementById(id)).filter(Boolean),storyMedia=['[data-product-card="tender"] .pc-img-wrap img','.story-visual .sv-img','.trade-route.trade-buy img','.trade-route.trade-supply img','#people-of-nariyal .ns-people-visual img','.promise-portal.coast img','.promise-portal.grove img','.promise-portal.farm img'].map(pathOf).filter(Boolean);return {main:visible(document.getElementById('main-site')||document.querySelector('main')),hero:visible(document.querySelector('.hero')),proReady:!!pro&&window.__NS_V421_PRO_CINE?.ready===true,legacyHidden:legacy.every(e=>e.hidden||getComputedStyle(e).display==='none'),retiredMotionHidden:!motion||motion.hidden||getComputedStyle(motion).display==='none',harvestSrc:harvest?.getAttribute('src')||'',mediaDiversityReady:document.documentElement.dataset.nsMediaDiversity==='ready',storyMedia,storyMediaUnique:[...new Set(storyMedia)],viewport:innerWidth,scrollWidth:Math.max(document.documentElement.scrollWidth,document.body.scrollWidth),productCount:productImgs.length,productSrcs:[...new Set(productImgs.map(i=>new URL(i.currentSrc||i.src,location.href).pathname))]};});
  if(!first.main)fail(vp.label,'main storefront is hidden');if(!first.hero)fail(vp.label,'homepage hero is hidden');if(!first.proReady)fail(vp.label,'approved professional cinematic did not initialize');if(!first.legacyHidden)fail(vp.label,'legacy cinematic remains visible');if(!first.retiredMotionHidden)fail(vp.label,'retired live-motion fold remains visible');if(first.harvestSrc&&!first.harvestSrc.startsWith('/assets/')&&!first.harvestSrc.startsWith('assets/'))fail(vp.label,`harvest media still points to retired deploy URL: ${first.harvestSrc}`);if(first.scrollWidth>first.viewport+6)fail(vp.label,`horizontal overflow ${first.scrollWidth}px > ${first.viewport}px`);if(first.productCount>=3&&first.productSrcs.length<3)fail(vp.label,`product imagery is repeating: ${JSON.stringify(first.productSrcs)}`);if(!first.mediaDiversityReady)fail(vp.label,'storefront media-diversity pass did not initialize');if(first.storyMedia.length>=8&&first.storyMediaUnique.length<6)fail(vp.label,`story imagery is too repetitive: ${JSON.stringify(first.storyMedia)}`);
  const height=await page.evaluate(()=>document.documentElement.scrollHeight);for(let y=0;y<height;y+=Math.max(620,Math.floor(vp.height*.75))){await page.evaluate(y=>scrollTo(0,y),y);await page.waitForTimeout(35);}const broken=await page.evaluate(()=>[...document.images].filter(img=>{const r=img.getBoundingClientRect(),s=getComputedStyle(img);return s.display!=='none'&&s.visibility!=='hidden'&&!img.hidden&&img.complete&&img.naturalWidth===0&&r.width>2&&r.height>2;}).map(i=>i.getAttribute('src')||i.src));if(broken.length)fail(vp.label,`visible broken images: ${JSON.stringify(broken.slice(0,12))}`);if(errors.length)fail(vp.label,`page errors: ${errors.join(' | ')}`);await page.evaluate(()=>scrollTo(0,0));await page.screenshot({path:path.join(OUT,`storefront-${vp.label}.png`),fullPage:false,timeout:10000});await context.close();
 }
 const context=await browser.newContext({viewport:{width:1440,height:900},serviceWorkers:'block'}),admin=await context.newPage(),adminErrors=[];admin.on('pageerror',e=>adminErrors.push(String(e)));await admin.goto(BASE+'/admin.html',{waitUntil:'domcontentloaded',timeout:30000});await admin.waitForURL(/admin-preview\.html/,{timeout:10000});await admin.waitForSelector('.ap-app',{state:'visible',timeout:12000});await admin.waitForFunction(()=>window.__NS_V421_ADMIN_V38__===true&&window.__NS_V421_ADMIN_V40__===true&&window.__NS_V421_ADMIN_RUNTIME_STABILITY__===true,null,{timeout:12000});await admin.waitForSelector('#apV38ImageTargets',{state:'attached',timeout:8000});const localState=await admin.evaluate(()=>{const labels=[...document.querySelectorAll('#apNav button')].map(b=>b.textContent.replace(/\s+/g,' ').trim()),required=['Overview','Orders','Payments','Products & Pricing','Inventory','Warehouses / Nodes','Delivery Services','Delivery Hub','Customer 360','Media Library','Schedule','Team / Tasks'];return {title:document.title,missing:required.filter(x=>!labels.some(v=>v.includes(x))),sidebar:!!document.querySelector('#apSidebar'),app:!!document.querySelector('.ap-app'),v38:window.__NS_V421_ADMIN_V38__===true,imageTargets:!!document.querySelector('#apV38ImageTargets')};});if(!/Business Command Center/i.test(localState.title)||!localState.sidebar||!localState.app||!localState.v38||!localState.imageTargets||localState.missing.length)fail('admin-local',`approved Business Command Center UI missing ${JSON.stringify(localState)}`);if(adminErrors.length)fail('admin-local',`runtime errors: ${adminErrors.join(' | ')}`);await admin.screenshot({path:path.join(OUT,'admin-business-command-center-local.png'),fullPage:false,timeout:10000});await context.close();
} finally {await browser.close().catch(()=>{});server.kill();}
const report={generatedAt:new Date().toISOString(),failures};fs.writeFileSync(path.join(OUT,'release-candidate-report.json'),JSON.stringify(report,null,2));console.log(`RELEASE CANDIDATE QA: ${failures.length?'FAIL':'PASS'}`);for(const f of failures)console.error(`FAIL [${f.scope}] ${f.msg}`);if(failures.length)process.exit(1);
