import { chromium } from 'playwright';
import { spawn } from 'node:child_process';
import fs from 'node:fs';
const must=(cond,msg)=>{if(!cond)throw new Error(msg)};
function staticGate(){
 const read=p=>fs.readFileSync(p,'utf8');
 const admin=read('admin.html'),bridge=read('assets/js/v421-production-bridge.js'),signer=read('netlify/functions/media-sign-upload.js'),v32=read('assets/js/v32-public.js'),mediaDb=read('assets/js/v421-media-db.js'),v30=read('assets/js/v30-page-sequencing.js'),controls=read('assets/js/v421-content-controls.js');
 must(!admin.includes('admin-live-legacy.html'),'Production /admin still routes to the rejected legacy Admin');
 must(admin.includes('admin-preview.html'),'Production /admin does not route to Business Command Center');
 must(bridge.includes("publicStories','site-config")&&bridge.includes("contentStories','admin-state"),'Production Admin remote configuration bridge is missing');
 must(bridge.includes('uploadMedia')&&bridge.includes('persistCatalog'),'Production Admin is missing durable media/catalog persistence');
 must(signer.includes('verifyFirebaseToken')&&signer.includes('adminRole')&&/authorization/i.test(signer),'Cloudinary signer is not protected by Firebase Admin authentication');
 must(v32.includes('#v421-cinematic-film-pro'),'Homepage Admin sequencing does not control the real professional cinematic');
 must(mediaDb.includes('admin-v38-production.js')&&mediaDb.includes('v421-production-bridge.js'),'Business Command Center production scripts are not loaded');
 must(v30.includes('v421-content-controls.js')&&v30.includes('v421-production-bridge.js'),'Public pages do not load shared remote content configuration');
 must(controls.includes('sectionMedia')&&controls.includes('renderCustom'),'Public image placement/custom section runtime is missing');
 console.log('STATIC PRODUCTION ADMIN GATE: PASS');
}
staticGate();
const server=spawn('python3',['-m','http.server','4175','--bind','127.0.0.1'],{stdio:'ignore'});
await new Promise(r=>setTimeout(r,1000));
const browser=await chromium.launch({headless:true});
const context=await browser.newContext({viewport:{width:1440,height:900},serviceWorkers:'block'});
const admin=await context.newPage();const errors=[];admin.on('pageerror',e=>errors.push('admin: '+e));
const home=await context.newPage();home.on('pageerror',e=>errors.push('home: '+e));
try{
 await admin.goto('http://127.0.0.1:4175/admin.html',{waitUntil:'domcontentloaded',timeout:30000});
 await admin.waitForURL(/admin-preview\.html/,{timeout:10000});
 await admin.waitForSelector('.ap-app',{state:'visible',timeout:10000});
 await admin.waitForFunction(()=>window.__NSV21_ADMIN_BOUND===true,null,{timeout:10000});
 await admin.waitForFunction(()=>window.__NS_V421_ADMIN_V38__===true,null,{timeout:10000});
 const baseline=await admin.evaluate(()=>JSON.stringify(window.NSV421Store.load()));
 await admin.locator('#apNav button[data-view="pages"]').click();
 await admin.waitForSelector('#apV38ImageTargets',{state:'visible',timeout:10000});
 must(await admin.locator('[data-sec-down="cinematic"]').count()===1,'Cinematic move control is missing in Admin');
 must(await admin.locator('[data-sec-vis="cinematic"]').count()===1,'Cinematic visibility control is missing in Admin');
 await admin.locator('[data-sec-down="cinematic"]').click();
 const savedOrder=await admin.evaluate(()=>[...(window.NSV421Store.load().sections||[])].sort((a,b)=>(+a.order||999)-(+b.order||999)).map(x=>({id:x.id,order:x.order,visible:x.visible!==false})));
 console.log('ADMIN SAVED ORDER AFTER CINEMATIC DOWN',JSON.stringify(savedOrder));
 await home.goto('http://127.0.0.1:4175/',{waitUntil:'domcontentloaded',timeout:30000});
 await home.waitForTimeout(900);await home.evaluate(()=>{try{window.skipIntro?.()}catch{} document.getElementById('jungle-intro')?.classList.add('ji-done')});
 await home.waitForSelector('#v421-cinematic-film-pro[data-ready="1"]',{timeout:20000});await home.waitForTimeout(1800);
 const orderDebug=await home.evaluate(()=>{
  const storeOrder=[...(window.NSV421Store?.load()?.sections||[])].sort((a,b)=>(+a.order||999)-(+b.order||999)).map(x=>({id:x.id,order:x.order,visible:x.visible!==false}));
  const top=[...document.querySelectorAll('#main-site > *')].map((x,i)=>({i,id:x.id||'',cls:typeof x.className==='string'?x.className:'',v32:x.dataset?.v32Section||'',hidden:!!x.hidden}));
  const film=document.getElementById('v421-cinematic-film-pro');
  const info=film?{parentId:film.parentElement?.id||'',parentClass:film.parentElement?.className||'',prevId:film.previousElementSibling?.id||'',prevClass:film.previousElementSibling?.className||'',prevV32:film.previousElementSibling?.dataset?.v32Section||'',nextId:film.nextElementSibling?.id||'',nextClass:film.nextElementSibling?.className||'',nextV32:film.nextElementSibling?.dataset?.v32Section||'',ready:film.dataset.ready||'',v32:film.dataset.v32Section||'',hidden:!!film.hidden}:null;
  return {storeOrder,top,film:info};
 });
 console.log('CINEMATIC ORDER DEBUG',JSON.stringify(orderDebug));
 await home.screenshot({path:'qa-artifacts/cinematic-order-debug.png',fullPage:false});
 let order=orderDebug.top.filter(x=>x.v32).map(x=>x.v32);
 must(order.indexOf('cinematic')>order.indexOf('brand-moment'),'Admin cinematic move did not change the real storefront module order');
 await admin.locator('[data-sec-vis="cinematic"]').click();await home.reload({waitUntil:'domcontentloaded'});await home.waitForTimeout(1500);
 const cineHidden=await home.evaluate(()=>{const e=document.getElementById('v421-cinematic-film-pro');return !!e&&(e.hidden||getComputedStyle(e).display==='none')});
 must(cineHidden,'Admin cinematic visibility toggle does not control the professional cinematic');
 await admin.locator('[data-sec-vis="cinematic"]').click();await admin.locator('[data-sec-up="cinematic"]').click();
 await admin.locator('[data-v38-target="homepage-people-teaser"]').click();await admin.waitForSelector('[data-v38-pick]',{state:'visible'});
 const picked=await admin.locator('[data-v38-pick]').first().getAttribute('data-v38-pick');await admin.locator('[data-v38-pick]').first().click();
 const pickedSrc=await admin.evaluate(id=>window.NSV421Store.mediaById(window.NSV421Store.load(),id)?.src||'',picked);
 await home.reload({waitUntil:'domcontentloaded'});await home.waitForTimeout(1200);
 const peopleSrc=await home.locator('#people-of-nariyal .ns-people-visual img').getAttribute('src');must(peopleSrc===pickedSrc,`Admin image change did not reach storefront (${peopleSrc} != ${pickedSrc})`);
 await admin.locator('[data-custom-edit="CS-PEOPLE"]').click();await admin.waitForSelector('#v21SectionForm');const qaTitle='QA People Stories '+Date.now();await admin.locator('#v21SectionForm input[name="title"]').fill(qaTitle);await admin.locator('#v21SectionForm button[type="submit"]').click();
 await home.reload({waitUntil:'domcontentloaded'});await home.waitForTimeout(1200);must((await home.locator('#ns-face-title').innerText()).includes(qaTitle),'Admin section edit did not update storefront content');
 await admin.waitForSelector('#apV30PageSelect');await admin.locator('#apV30PageSelect').selectOption('fresh-tender-coconut.html');await admin.waitForSelector('#apV30PageRows [data-down]');const firstKey=await admin.locator('#apV30PageRows [data-down]').first().getAttribute('data-down');await admin.locator('#apV30PageRows [data-down]').first().click();
 const product=await context.newPage();await product.goto('http://127.0.0.1:4175/fresh-tender-coconut.html',{waitUntil:'domcontentloaded'});await product.waitForTimeout(900);const seq=await product.evaluate(()=>[...document.querySelectorAll('[data-v30-seq-key]')].filter(x=>!x.hidden).map(x=>x.dataset.v30SeqKey));must(seq.indexOf(firstKey)===1,'Admin non-home page move did not change public page section order');await product.close();
 await admin.locator('#apNav button[data-view="team"]').click();admin.once('dialog',d=>d.accept('QA persistence task'));await admin.locator('#apTaskAdd').click();must(await admin.evaluate(()=>window.NSV421Store.load().tasks.some(t=>t.title==='QA persistence task')),'Task creation did not save');await admin.reload({waitUntil:'domcontentloaded'});await admin.waitForTimeout(700);must(await admin.evaluate(()=>window.NSV421Store.load().tasks.some(t=>t.title==='QA persistence task')),'Task did not survive Admin reload');
 await home.goto('http://127.0.0.1:4175/',{waitUntil:'domcontentloaded'});await home.waitForTimeout(1000);const visualSources=await home.evaluate(()=>['.hero-visual img','#harvest-film .harvest-scene:nth-child(1)','#harvest-film .harvest-scene:nth-child(2)','#harvest-film .harvest-scene:nth-child(3)','#people-of-nariyal .ns-people-visual img'].map(s=>document.querySelector(s)?.getAttribute('src')||'').filter(Boolean));const counts=visualSources.reduce((a,s)=>(a[s]=(a[s]||0)+1,a),{});must(Object.values(counts).every(n=>n===1),`Major homepage images repeat: ${JSON.stringify(counts)}`);
 const broken=await home.evaluate(()=>[...document.images].filter(i=>i.getBoundingClientRect().width>10&&i.getBoundingClientRect().height>10&&i.complete&&i.naturalWidth===0).map(i=>i.src));must(!broken.length,'Visible broken images found: '+broken.join(','));
 await admin.screenshot({path:'qa-artifacts/admin-functional-readiness.png',fullPage:false});
 await admin.evaluate(raw=>{localStorage.setItem(window.NSV421Store.KEY,raw);window.dispatchEvent(new CustomEvent('nsv421:change'));},baseline);
 if(errors.length)throw new Error('Page errors: '+errors.join(' | '));console.log('ADMIN FUNCTIONAL READINESS QA: PASS');
} finally { await browser.close();server.kill(); }
